## Autogenerated by Scribe. DO NOT MODIFY.

name: '40. Notifications'
description: |-

  What the platform has told **you**: a manufacturing order finished, a
  quality sheet approved, a delivery minute circulated.

  Every endpoint here is **self-scoped** and carries no permission. That is
  deliberate and it is the safest possible rule: a notification is addressed
  to one user, so the only question is whether you are that user. There is no
  "read someone else's notifications" endpoint to get the authorization wrong
  on.

  These are the same rows the admin panel's bell shows, so marking one read on
  a phone clears it on the desktop.

  The body of a notification is the panel's own payload — `title`, `body`,
  `icon`, `color`, `actions`. It is passed through as `data` rather than
  reshaped, because the panel's notification format is what every producer in
  the platform already writes, and translating it here would mean a second
  format to keep in step.
endpoints:
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/notifications
    metadata:
      custom: []
      groupName: '40. Notifications'
      groupDescription: |-

        What the platform has told **you**: a manufacturing order finished, a
        quality sheet approved, a delivery minute circulated.

        Every endpoint here is **self-scoped** and carries no permission. That is
        deliberate and it is the safest possible rule: a notification is addressed
        to one user, so the only question is whether you are that user. There is no
        "read someone else's notifications" endpoint to get the authorization wrong
        on.

        These are the same rows the admin panel's bell shows, so marking one read on
        a phone clears it on the desktop.

        The body of a notification is the panel's own payload — `title`, `body`,
        `icon`, `color`, `actions`. It is passed through as `data` rather than
        reshaped, because the panel's notification format is what every producer in
        the platform already writes, and translating it here would mean a second
        format to keep in step.
      subgroup: ''
      subgroupDescription: ''
      title: 'List your notifications'
      description: |-
        Newest first. `filter[unread]=true` narrows to what you have not seen,
        which is what a badge and a pull-to-refresh both want.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters:
      page:
        custom: []
        name: page
        description: 'The page number.'
        required: false
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      per_page:
        custom: []
        name: per_page
        description: 'Rows per page, max 100.'
        required: false
        example: 25
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      'filter[unread]':
        custom: []
        name: 'filter[unread]'
        description: 'Only unread notifications.'
        required: false
        example: true
        type: boolean
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanQueryParameters:
      page: 1
      per_page: 25
      'filter[unread]': true
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":[{"id":"9f1c2d3e","type":"App\\Notifications\\ManufacturingFinished","data":{"title":"Manufacturing finished","body":"WO-202609-0004 is ready for delivery"},"read":false,"read_at":null,"created_at":"2026-09-12T16:30:00+00:00"}],"meta":{"request_id":"9f1c","api_version":"1","pagination":{"total":1,"count":1,"per_page":25,"current_page":1,"total_pages":1}},"links":{"first":"","prev":null,"next":null,"last":""}}'
        headers: []
        description: Success
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/notifications/unread-count
    metadata:
      custom: []
      groupName: '40. Notifications'
      groupDescription: |-

        What the platform has told **you**: a manufacturing order finished, a
        quality sheet approved, a delivery minute circulated.

        Every endpoint here is **self-scoped** and carries no permission. That is
        deliberate and it is the safest possible rule: a notification is addressed
        to one user, so the only question is whether you are that user. There is no
        "read someone else's notifications" endpoint to get the authorization wrong
        on.

        These are the same rows the admin panel's bell shows, so marking one read on
        a phone clears it on the desktop.

        The body of a notification is the panel's own payload — `title`, `body`,
        `icon`, `color`, `actions`. It is passed through as `data` rather than
        reshaped, because the panel's notification format is what every producer in
        the platform already writes, and translating it here would mean a second
        format to keep in step.
      subgroup: ''
      subgroupDescription: ''
      title: 'How many you have not read'
      description: |-
        The cheapest call in the API — one indexed count. Intended for a badge,
        so it is safe to call on every foreground.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":{"unread":3},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: Success
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - POST
    uri: 'api/v1/notifications/{notification}/read'
    metadata:
      custom: []
      groupName: '40. Notifications'
      groupDescription: |-

        What the platform has told **you**: a manufacturing order finished, a
        quality sheet approved, a delivery minute circulated.

        Every endpoint here is **self-scoped** and carries no permission. That is
        deliberate and it is the safest possible rule: a notification is addressed
        to one user, so the only question is whether you are that user. There is no
        "read someone else's notifications" endpoint to get the authorization wrong
        on.

        These are the same rows the admin panel's bell shows, so marking one read on
        a phone clears it on the desktop.

        The body of a notification is the panel's own payload — `title`, `body`,
        `icon`, `color`, `actions`. It is passed through as `data` rather than
        reshaped, because the panel's notification format is what every producer in
        the platform already writes, and translating it here would mean a second
        format to keep in step.
      subgroup: ''
      subgroupDescription: ''
      title: 'Mark one as read'
      description: |-
        Scoped to your own notifications: another user's id is a **404**, not a
        403. Answering "forbidden" would confirm that the notification exists,
        which is more than a caller who cannot read it should learn.

        Marking an already-read notification is a silent success.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      notification:
        custom: []
        name: notification
        description: 'The notification id.'
        required: true
        example: 9f1c2d3e-4b5a-6789-abcd-ef0123456789
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      notification: 9f1c2d3e-4b5a-6789-abcd-ef0123456789
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":{"id":"9f1c2d3e","read":true,"read_at":"2026-09-12T18:00:00+00:00"},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: Marked
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - POST
    uri: api/v1/notifications/read-all
    metadata:
      custom: []
      groupName: '40. Notifications'
      groupDescription: |-

        What the platform has told **you**: a manufacturing order finished, a
        quality sheet approved, a delivery minute circulated.

        Every endpoint here is **self-scoped** and carries no permission. That is
        deliberate and it is the safest possible rule: a notification is addressed
        to one user, so the only question is whether you are that user. There is no
        "read someone else's notifications" endpoint to get the authorization wrong
        on.

        These are the same rows the admin panel's bell shows, so marking one read on
        a phone clears it on the desktop.

        The body of a notification is the panel's own payload — `title`, `body`,
        `icon`, `color`, `actions`. It is passed through as `data` rather than
        reshaped, because the panel's notification format is what every producer in
        the platform already writes, and translating it here would mean a second
        format to keep in step.
      subgroup: ''
      subgroupDescription: ''
      title: 'Mark everything as read'
      description: |-
        Returns how many were actually changed, so a client can say "3 cleared"
        rather than guessing.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":{"marked":3,"unread":0},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: Cleared
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - DELETE
    uri: 'api/v1/notifications/{id}'
    metadata:
      custom: []
      groupName: '40. Notifications'
      groupDescription: |-

        What the platform has told **you**: a manufacturing order finished, a
        quality sheet approved, a delivery minute circulated.

        Every endpoint here is **self-scoped** and carries no permission. That is
        deliberate and it is the safest possible rule: a notification is addressed
        to one user, so the only question is whether you are that user. There is no
        "read someone else's notifications" endpoint to get the authorization wrong
        on.

        These are the same rows the admin panel's bell shows, so marking one read on
        a phone clears it on the desktop.

        The body of a notification is the panel's own payload — `title`, `body`,
        `icon`, `color`, `actions`. It is passed through as `data` rather than
        reshaped, because the panel's notification format is what every producer in
        the platform already writes, and translating it here would mean a second
        format to keep in step.
      subgroup: ''
      subgroupDescription: ''
      title: 'Delete one of your notifications'
      description: "Self-scoped like the rest: another user's id is a 404."
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      id:
        custom: []
        name: id
        description: 'The ID of the notification.'
        required: true
        example: consequatur
        type: string
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      notification:
        custom: []
        name: notification
        description: 'The notification id.'
        required: true
        example: 9f1c2d3e-4b5a-6789-abcd-ef0123456789
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      id: consequatur
      notification: 9f1c2d3e-4b5a-6789-abcd-ef0123456789
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 204
        content: '{}'
        headers: []
        description: Deleted
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
