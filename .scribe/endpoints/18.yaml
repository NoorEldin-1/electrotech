name: '21. Issue vouchers'
description: |-

  إذن صرف — material leaving the raw store for a work order.

  A voucher is always raised **against a work order**, and the server
  pre-fills it with what that order still needs: its material plan, minus what
  other vouchers already carry, net of posted returns. The warehouse then
  edits the list and posts it. Posting transfers every line from raw materials
  into work-in-progress and loads the value onto the operation.

  The rule that shapes this whole module is the **excess gate**. A voucher
  that would take the order past its plan is refused with
  `422 issue_excess_requires_approval`, carrying the offending rows in
  `details.excess`. Going over is allowed — a broken part has to be replaced —
  but only as a decision: a user holding `issue_vouchers.approve_excess`
  retries with `allow_excess` and a written reason, and both are stamped on
  the document.

  Only POSTED vouchers count as already-issued at that gate. A sibling draft
  has not moved anything, so it cannot make a voucher "excess". Drafts *are*
  counted when suggesting lines, so two store keepers do not each prepare a
  voucher for the same remaining material.
endpoints:
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/issue-vouchers
    metadata:
      custom: []
      groupName: '21. Issue vouchers'
      groupDescription: |-

        إذن صرف — material leaving the raw store for a work order.

        A voucher is always raised **against a work order**, and the server
        pre-fills it with what that order still needs: its material plan, minus what
        other vouchers already carry, net of posted returns. The warehouse then
        edits the list and posts it. Posting transfers every line from raw materials
        into work-in-progress and loads the value onto the operation.

        The rule that shapes this whole module is the **excess gate**. A voucher
        that would take the order past its plan is refused with
        `422 issue_excess_requires_approval`, carrying the offending rows in
        `details.excess`. Going over is allowed — a broken part has to be replaced —
        but only as a decision: a user holding `issue_vouchers.approve_excess`
        retries with `allow_excess` and a written reason, and both are stamped on
        the document.

        Only POSTED vouchers count as already-issued at that gate. A sibling draft
        has not moved anything, so it cannot make a voucher "excess". Drafts *are*
        counted when suggesting lines, so two store keepers do not each prepare a
        voucher for the same remaining material.
      subgroup: ''
      subgroupDescription: ''
      title: 'List issue vouchers'
      description: ''
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters:
      page:
        custom: []
        name: page
        description: 'The page number.'
        required: false
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      per_page:
        custom: []
        name: per_page
        description: 'Rows per page, max 100.'
        required: false
        example: 25
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      search:
        custom: []
        name: search
        description: 'Matches the voucher number.'
        required: false
        example: IV-2026
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      'filter[status]':
        custom: []
        name: 'filter[status]'
        description: 'draft or posted.'
        required: false
        example: posted
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      'filter[work_order]':
        custom: []
        name: 'filter[work_order]'
        description: 'Work order id.'
        required: false
        example: 31
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      'filter[has_excess]':
        custom: []
        name: 'filter[has_excess]'
        description: 'Only vouchers that went over the plan.'
        required: false
        example: true
        type: boolean
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      'filter[voucher_date]':
        custom: []
        name: 'filter[voucher_date]'
        description: 'Inclusive date range from,until.'
        required: false
        example: '2026-01-01,2026-06-30'
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      sort:
        custom: []
        name: sort
        description: 'Allowed: voucher_number, voucher_date, total_value, created_at.'
        required: false
        example: '-voucher_date'
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      include:
        custom: []
        name: include
        description: 'Allowed: workOrder.'
        required: false
        example: workOrder
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanQueryParameters:
      page: 1
      per_page: 25
      search: IV-2026
      'filter[status]': posted
      'filter[work_order]': 31
      'filter[has_excess]': true
      'filter[voucher_date]': '2026-01-01,2026-06-30'
      sort: '-voucher_date'
      include: workOrder
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":[{"id":48,"type":"issue_voucher","voucher_number":"IV-202609-0012","status":{"value":"posted","label":"Posted","color":"success"},"total_value":"40000.00","excess":{"has_excess":false},"lines_count":3}],"meta":{"request_id":"9f1c","api_version":"1","pagination":{"total":1,"count":1,"per_page":25,"current_page":1,"total_pages":1}},"links":{"first":"","prev":null,"next":null,"last":""}}'
        headers: []
        description: Success
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: 'api/v1/issue-vouchers/{issue_voucher_id}'
    metadata:
      custom: []
      groupName: '21. Issue vouchers'
      groupDescription: |-

        إذن صرف — material leaving the raw store for a work order.

        A voucher is always raised **against a work order**, and the server
        pre-fills it with what that order still needs: its material plan, minus what
        other vouchers already carry, net of posted returns. The warehouse then
        edits the list and posts it. Posting transfers every line from raw materials
        into work-in-progress and loads the value onto the operation.

        The rule that shapes this whole module is the **excess gate**. A voucher
        that would take the order past its plan is refused with
        `422 issue_excess_requires_approval`, carrying the offending rows in
        `details.excess`. Going over is allowed — a broken part has to be replaced —
        but only as a decision: a user holding `issue_vouchers.approve_excess`
        retries with `allow_excess` and a written reason, and both are stamped on
        the document.

        Only POSTED vouchers count as already-issued at that gate. A sibling draft
        has not moved anything, so it cannot make a voucher "excess". Drafts *are*
        counted when suggesting lines, so two store keepers do not each prepare a
        voucher for the same remaining material.
      subgroup: ''
      subgroupDescription: ''
      title: 'Show an issue voucher'
      description: ''
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      issue_voucher_id:
        custom: []
        name: issue_voucher_id
        description: 'The ID of the issue voucher.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      issue_voucher:
        custom: []
        name: issue_voucher
        description: 'The voucher id.'
        required: true
        example: 48
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      issue_voucher_id: 1
      issue_voucher: 48
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":{"id":48,"type":"issue_voucher","voucher_number":"IV-202609-0012","status":{"value":"draft","label":"Draft","color":"gray"},"lines":[{"item_id":7,"quantity":"40.0000","unit_cost":"1000.00","line_value":"40000.00"}]},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: Success
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: 'api/v1/issue-vouchers/{issue_voucher_id}/excess'
    metadata:
      custom: []
      groupName: '21. Issue vouchers'
      groupDescription: |-

        إذن صرف — material leaving the raw store for a work order.

        A voucher is always raised **against a work order**, and the server
        pre-fills it with what that order still needs: its material plan, minus what
        other vouchers already carry, net of posted returns. The warehouse then
        edits the list and posts it. Posting transfers every line from raw materials
        into work-in-progress and loads the value onto the operation.

        The rule that shapes this whole module is the **excess gate**. A voucher
        that would take the order past its plan is refused with
        `422 issue_excess_requires_approval`, carrying the offending rows in
        `details.excess`. Going over is allowed — a broken part has to be replaced —
        but only as a decision: a user holding `issue_vouchers.approve_excess`
        retries with `allow_excess` and a written reason, and both are stamped on
        the document.

        Only POSTED vouchers count as already-issued at that gate. A sibling draft
        has not moved anything, so it cannot make a voucher "excess". Drafts *are*
        counted when suggesting lines, so two store keepers do not each prepare a
        voucher for the same remaining material.
      subgroup: ''
      subgroupDescription: ''
      title: 'Preview the excess on a draft voucher'
      description: |-
        The same comparison `post` runs, without posting: the rows this voucher
        goes over on, if any. An empty list means the voucher will post without
        needing an approval.

        Worth calling before showing the post button, so the user learns what
        they are about to be asked before they are asked it.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      issue_voucher_id:
        custom: []
        name: issue_voucher_id
        description: 'The ID of the issue voucher.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      issue_voucher:
        custom: []
        name: issue_voucher
        description: 'The voucher id.'
        required: true
        example: 48
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      issue_voucher_id: 1
      issue_voucher: 48
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":[{"item_id":7,"item_name":"Copper busbar 120mm","required":"40.0000","previously_issued":"25.0000","remaining":"15.0000","this_voucher":"18.0000","excess":"3.0000"}],"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: 'Over the plan'
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - POST
    uri: api/v1/issue-vouchers
    metadata:
      custom: []
      groupName: '21. Issue vouchers'
      groupDescription: |-

        إذن صرف — material leaving the raw store for a work order.

        A voucher is always raised **against a work order**, and the server
        pre-fills it with what that order still needs: its material plan, minus what
        other vouchers already carry, net of posted returns. The warehouse then
        edits the list and posts it. Posting transfers every line from raw materials
        into work-in-progress and loads the value onto the operation.

        The rule that shapes this whole module is the **excess gate**. A voucher
        that would take the order past its plan is refused with
        `422 issue_excess_requires_approval`, carrying the offending rows in
        `details.excess`. Going over is allowed — a broken part has to be replaced —
        but only as a decision: a user holding `issue_vouchers.approve_excess`
        retries with `allow_excess` and a written reason, and both are stamped on
        the document.

        Only POSTED vouchers count as already-issued at that gate. A sibling draft
        has not moved anything, so it cannot make a voucher "excess". Drafts *are*
        counted when suggesting lines, so two store keepers do not each prepare a
        voucher for the same remaining material.
      subgroup: ''
      subgroupDescription: ''
      title: 'Open a draft issue voucher'
      description: |-
        Pre-filled from the work order with what it **still** needs, not its full
        plan: an order issued in two batches must not have the second voucher
        propose the whole recipe again. No stock moves — the warehouse reviews,
        adjusts the lines, then posts.

        Refused when the order has no material plan at all, and refused when
        everything it needs has already been issued. Those are different
        messages on purpose: the first is a planning problem, the second means
        there is simply nothing to do.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters:
      work_order_id:
        custom: []
        name: work_order_id
        description: 'The order to issue against.'
        required: true
        example: 31
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanBodyParameters:
      work_order_id: 31
    fileParameters: []
    responses:
      -
        custom: []
        status: 201
        content: '{"data":{"id":48,"type":"issue_voucher","voucher_number":"IV-202609-0012","status":{"value":"draft","label":"Draft","color":"gray"},"lines":[{"item_id":7,"quantity":"15.0000","unit_cost":"1000.00"}]},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: Created
      -
        custom: []
        status: 422
        content: '{"error":{"code":"business_rule_violated","message":"Nothing is left to issue for WO-202609-0004."},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: 'Nothing left to issue'
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - PUT
    uri: 'api/v1/issue-vouchers/{issue_voucher_id}/lines'
    metadata:
      custom: []
      groupName: '21. Issue vouchers'
      groupDescription: |-

        إذن صرف — material leaving the raw store for a work order.

        A voucher is always raised **against a work order**, and the server
        pre-fills it with what that order still needs: its material plan, minus what
        other vouchers already carry, net of posted returns. The warehouse then
        edits the list and posts it. Posting transfers every line from raw materials
        into work-in-progress and loads the value onto the operation.

        The rule that shapes this whole module is the **excess gate**. A voucher
        that would take the order past its plan is refused with
        `422 issue_excess_requires_approval`, carrying the offending rows in
        `details.excess`. Going over is allowed — a broken part has to be replaced —
        but only as a decision: a user holding `issue_vouchers.approve_excess`
        retries with `allow_excess` and a written reason, and both are stamped on
        the document.

        Only POSTED vouchers count as already-issued at that gate. A sibling draft
        has not moved anything, so it cannot make a voucher "excess". Drafts *are*
        counted when suggesting lines, so two store keepers do not each prepare a
        voucher for the same remaining material.
      subgroup: ''
      subgroupDescription: ''
      title: 'Replace the lines of a draft voucher'
      description: |-
        The whole list in one atomic request — see the work order's material
        endpoint for why replace beats merge over a weak link.

        `unit_cost` falls back to the item card when omitted. A posted voucher
        is immutable: its stock has already moved, so the correction is a return
        voucher, not an edit.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      issue_voucher_id:
        custom: []
        name: issue_voucher_id
        description: 'The ID of the issue voucher.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      issue_voucher:
        custom: []
        name: issue_voucher
        description: 'The voucher id.'
        required: true
        example: 48
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      issue_voucher_id: 1
      issue_voucher: 48
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters:
      lines:
        custom: []
        name: lines
        description: 'The complete list. Send `[]` to clear it.'
        required: true
        example:
          - []
        type: 'object[]'
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      'lines[].item_id':
        custom: []
        name: 'lines[].item_id'
        description: ''
        required: true
        example: 7
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      'lines[].quantity':
        custom: []
        name: 'lines[].quantity'
        description: ''
        required: true
        example: 15.0
        type: number
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      'lines[].unit_cost':
        custom: []
        name: 'lines[].unit_cost'
        description: "optional Defaults to the item's current cost."
        required: false
        example: 1000.0
        type: number
        enumValues: []
        exampleWasSpecified: true
        nullable: true
        deprecated: false
    cleanBodyParameters:
      lines:
        -
          item_id: 7
          quantity: 15.0
          unit_cost: 1000.0
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":{"id":48,"type":"issue_voucher","lines":[{"item_id":7,"quantity":"15.0000","unit_cost":"1000.00","line_value":"15000.00"}]},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: Replaced
      -
        custom: []
        status: 403
        content: '{"error":{"code":"forbidden","message":"You do not have permission to perform this action."},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: 'Already posted'
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - POST
    uri: 'api/v1/issue-vouchers/{issue_voucher_id}/post'
    metadata:
      custom: []
      groupName: '21. Issue vouchers'
      groupDescription: |-

        إذن صرف — material leaving the raw store for a work order.

        A voucher is always raised **against a work order**, and the server
        pre-fills it with what that order still needs: its material plan, minus what
        other vouchers already carry, net of posted returns. The warehouse then
        edits the list and posts it. Posting transfers every line from raw materials
        into work-in-progress and loads the value onto the operation.

        The rule that shapes this whole module is the **excess gate**. A voucher
        that would take the order past its plan is refused with
        `422 issue_excess_requires_approval`, carrying the offending rows in
        `details.excess`. Going over is allowed — a broken part has to be replaced —
        but only as a decision: a user holding `issue_vouchers.approve_excess`
        retries with `allow_excess` and a written reason, and both are stamped on
        the document.

        Only POSTED vouchers count as already-issued at that gate. A sibling draft
        has not moved anything, so it cannot make a voucher "excess". Drafts *are*
        counted when suggesting lines, so two store keepers do not each prepare a
        voucher for the same remaining material.
      subgroup: ''
      subgroupDescription: ''
      title: 'Post an issue voucher'
      description: |-
        The moment the material leaves the store. Every line is transferred from
        raw materials into work-in-progress, the total is loaded onto the
        operation (the project is the cost centre) and accrued onto the work
        order, and the voucher is signed — all in one transaction.

        **The excess flow.** Before any stock moves, the voucher is compared
        against what the order still needs. If it goes over, the response is
        `422 issue_excess_requires_approval` with the offending rows in
        `details.excess` — item by item: what was required, what was already
        issued, what is left, what this voucher asks for, and the overage. Show
        those rows. If the user holds `issue_vouchers.approve_excess`, offer to
        retry with `allow_excess: true` and a reason; otherwise the only way
        forward is to edit the lines down.

        Posting twice is refused, not ignored — which is what makes
        double-posting impossible even if the `Idempotency-Key` is fumbled.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      issue_voucher_id:
        custom: []
        name: issue_voucher_id
        description: 'The ID of the issue voucher.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      issue_voucher:
        custom: []
        name: issue_voucher
        description: 'The voucher id.'
        required: true
        example: 48
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      issue_voucher_id: 1
      issue_voucher: 48
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters:
      allow_excess:
        custom: []
        name: allow_excess
        description: 'optional Approve issuing more than the order still needs. Requires `issue_vouchers.approve_excess`.'
        required: false
        example: true
        type: boolean
        enumValues: []
        exampleWasSpecified: true
        nullable: true
        deprecated: false
      excess_reason:
        custom: []
        name: excess_reason
        description: 'required_if_accepted Why the excess is justified. Stamped on the voucher.'
        required: false
        example: 'Two busbars damaged during assembly'
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: true
        deprecated: false
    cleanBodyParameters:
      allow_excess: true
      excess_reason: 'Two busbars damaged during assembly'
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":{"id":48,"type":"issue_voucher","status":{"value":"posted","label":"Posted","color":"success"},"total_value":"15000.00","signed_at":"2026-09-12T09:15:00+00:00"},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: Posted
      -
        custom: []
        status: 422
        content: '{"error":{"code":"issue_excess_requires_approval","message":"Voucher IV-202609-0012 issues more than the order still needs.","details":{"excess":[{"item_id":7,"item_name":"Copper busbar 120mm","required":40,"previously_issued":25,"remaining":15,"this_voucher":18,"excess":3}]}},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: 'Excess needs approval'
      -
        custom: []
        status: 422
        content: '{"error":{"code":"business_rule_violated","message":"Insufficient stock for ''Copper busbar 120mm'' in Raw Materials. Available: 12, Requested: 40."},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: 'Insufficient stock'
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - DELETE
    uri: 'api/v1/issue-vouchers/{issue_voucher_id}'
    metadata:
      custom: []
      groupName: '21. Issue vouchers'
      groupDescription: |-

        إذن صرف — material leaving the raw store for a work order.

        A voucher is always raised **against a work order**, and the server
        pre-fills it with what that order still needs: its material plan, minus what
        other vouchers already carry, net of posted returns. The warehouse then
        edits the list and posts it. Posting transfers every line from raw materials
        into work-in-progress and loads the value onto the operation.

        The rule that shapes this whole module is the **excess gate**. A voucher
        that would take the order past its plan is refused with
        `422 issue_excess_requires_approval`, carrying the offending rows in
        `details.excess`. Going over is allowed — a broken part has to be replaced —
        but only as a decision: a user holding `issue_vouchers.approve_excess`
        retries with `allow_excess` and a written reason, and both are stamped on
        the document.

        Only POSTED vouchers count as already-issued at that gate. A sibling draft
        has not moved anything, so it cannot make a voucher "excess". Drafts *are*
        counted when suggesting lines, so two store keepers do not each prepare a
        voucher for the same remaining material.
      subgroup: ''
      subgroupDescription: ''
      title: 'Delete a draft issue voucher'
      description: |-
        Draft only. A posted voucher has already moved stock and loaded cost onto
        the operation; the correction for a mistake is a return voucher, not a
        deletion that leaves the ledger holding movements with no source.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      issue_voucher_id:
        custom: []
        name: issue_voucher_id
        description: 'The ID of the issue voucher.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      issue_voucher:
        custom: []
        name: issue_voucher
        description: 'The voucher id.'
        required: true
        example: 48
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      issue_voucher_id: 1
      issue_voucher: 48
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 204
        content: '{}'
        headers: []
        description: Deleted
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
