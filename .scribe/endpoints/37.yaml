name: '41. Activity log'
description: |-

  سجل النشاط — who changed what, and when.

  **Read-only, and append-only underneath.** `ActivityPolicy` answers false to
  create, update and delete, and there are no write routes. A log that could
  be edited is not a log.

  Each row carries the subject it is about (`subject_type` + `subject_id`), the
  user who caused it, and the `properties` bag holding the before/after values
  the model recorded. The properties are passed through as written: they are
  evidence, and reshaping evidence for presentation is how detail goes missing.

  The log grows without limit, so the index is filtered rather than browsed —
  `filter[subject_type]` and `filter[subject_id]` together answer "what
  happened to this record", which is the question that gets asked.
endpoints:
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/activity-log
    metadata:
      custom: []
      groupName: '41. Activity log'
      groupDescription: |-

        سجل النشاط — who changed what, and when.

        **Read-only, and append-only underneath.** `ActivityPolicy` answers false to
        create, update and delete, and there are no write routes. A log that could
        be edited is not a log.

        Each row carries the subject it is about (`subject_type` + `subject_id`), the
        user who caused it, and the `properties` bag holding the before/after values
        the model recorded. The properties are passed through as written: they are
        evidence, and reshaping evidence for presentation is how detail goes missing.

        The log grows without limit, so the index is filtered rather than browsed —
        `filter[subject_type]` and `filter[subject_id]` together answer "what
        happened to this record", which is the question that gets asked.
      subgroup: ''
      subgroupDescription: ''
      title: 'List activity'
      description: ''
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters:
      page:
        custom: []
        name: page
        description: 'The page number.'
        required: false
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      per_page:
        custom: []
        name: per_page
        description: 'Rows per page, max 100.'
        required: false
        example: 25
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      search:
        custom: []
        name: search
        description: 'Matches the description.'
        required: false
        example: approved
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      'filter[log_name]':
        custom: []
        name: 'filter[log_name]'
        description: ''
        required: false
        example: default
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      'filter[subject_type]':
        custom: []
        name: 'filter[subject_type]'
        description: "The subject's class name."
        required: false
        example: App\Models\WorkOrder
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      'filter[subject_id]':
        custom: []
        name: 'filter[subject_id]'
        description: "The subject's id."
        required: false
        example: 31
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      'filter[causer_id]':
        custom: []
        name: 'filter[causer_id]'
        description: 'Who did it.'
        required: false
        example: 9
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      'filter[event]':
        custom: []
        name: 'filter[event]'
        description: 'created, updated or deleted.'
        required: false
        example: updated
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      'filter[created_at]':
        custom: []
        name: 'filter[created_at]'
        description: 'Inclusive date range from,until.'
        required: false
        example: '2026-09-01,2026-09-30'
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      sort:
        custom: []
        name: sort
        description: 'Allowed: created_at, id.'
        required: false
        example: '-created_at'
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanQueryParameters:
      page: 1
      per_page: 25
      search: approved
      'filter[log_name]': default
      'filter[subject_type]': App\Models\WorkOrder
      'filter[subject_id]': 31
      'filter[causer_id]': 9
      'filter[event]': updated
      'filter[created_at]': '2026-09-01,2026-09-30'
      sort: '-created_at'
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":[{"id":4120,"log_name":"default","description":"WO #WO-202609-0004 was updated","event":"updated","subject_type":"App\\Models\\WorkOrder","subject_id":31,"causer_id":9,"properties":{"attributes":{"status":"in_progress"},"old":{"status":"pending"}},"created_at":"2026-09-12T08:00:00+00:00"}],"meta":{"request_id":"9f1c","api_version":"1","pagination":{"total":1,"count":1,"per_page":25,"current_page":1,"total_pages":1}},"links":{"first":"","prev":null,"next":null,"last":""}}'
        headers: []
        description: Success
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
