name: '12. Sales pipeline'
description: |-

  Moving an operation between stages:

      Draft → Tender → In Hand → In Progress (active operation)
                            ↓
                          Lost

  These are separate endpoints rather than a `status` field on PATCH for two
  reasons. Each move has its own permission (`projects.move_to_active` is not
  `projects.edit`), and each has pre-conditions the caller cannot see —
  moving to Active needs both the consultant's acceptance email and the
  manager's approval on file.

  Every rule here lives in App\Services\SalesPipelineService, which the
  Filament panel calls too. A pre-condition failure arrives as
  **422 `business_rule_violated`** with a message written for a person: show
  it to the user directly rather than mapping it.
endpoints:
  -
    custom: []
    httpMethods:
      - POST
    uri: 'api/v1/projects/{project_id}/move-to-tender'
    metadata:
      custom: []
      groupName: '12. Sales pipeline'
      groupDescription: |-

        Moving an operation between stages:

            Draft → Tender → In Hand → In Progress (active operation)
                                  ↓
                                Lost

        These are separate endpoints rather than a `status` field on PATCH for two
        reasons. Each move has its own permission (`projects.move_to_active` is not
        `projects.edit`), and each has pre-conditions the caller cannot see —
        moving to Active needs both the consultant's acceptance email and the
        manager's approval on file.

        Every rule here lives in App\Services\SalesPipelineService, which the
        Filament panel calls too. A pre-condition failure arrives as
        **422 `business_rule_violated`** with a message written for a person: show
        it to the user directly rather than mapping it.
      subgroup: ''
      subgroupDescription: ''
      title: 'Move to Tender'
      description: |-
        Requires at least one recorded offer, so the Tender list always carries
        a meaningful current price.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      project_id:
        custom: []
        name: project_id
        description: 'The ID of the project.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      project:
        custom: []
        name: project
        description: 'The operation id.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      project_id: 1
      project: 1
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":{"id":1,"type":"project","status":{"value":"tender","label":"Tender","color":"info"}},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: Success
      -
        custom: []
        status: 422
        content: '{"error":{"code":"business_rule_violated","message":"Cannot move to Tender: at least one offer must be recorded first."},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: 'No offer yet'
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - POST
    uri: 'api/v1/projects/{project_id}/move-to-in-hand'
    metadata:
      custom: []
      groupName: '12. Sales pipeline'
      groupDescription: |-

        Moving an operation between stages:

            Draft → Tender → In Hand → In Progress (active operation)
                                  ↓
                                Lost

        These are separate endpoints rather than a `status` field on PATCH for two
        reasons. Each move has its own permission (`projects.move_to_active` is not
        `projects.edit`), and each has pre-conditions the caller cannot see —
        moving to Active needs both the consultant's acceptance email and the
        manager's approval on file.

        Every rule here lives in App\Services\SalesPipelineService, which the
        Filament panel calls too. A pre-condition failure arrives as
        **422 `business_rule_violated`** with a message written for a person: show
        it to the user directly rather than mapping it.
      subgroup: ''
      subgroupDescription: ''
      title: 'Move to In Hand'
      description: |-
        The client has accepted in principle and asked for the SMB (the
        operation's Submittal document). The resulting `smb_status` is derived
        from whether that document is already attached — upload it through the
        attachments endpoint with `category=submittal` before or after this call.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      project_id:
        custom: []
        name: project_id
        description: 'The ID of the project.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      project:
        custom: []
        name: project
        description: 'The operation id.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      project_id: 1
      project: 1
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":{"id":1,"type":"project","status":{"value":"in_hand","label":"In Hand","color":"warning"},"smb_status":"pending","has_smb":false},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: Success
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - POST
    uri: 'api/v1/projects/{project_id}/move-to-active'
    metadata:
      custom: []
      groupName: '12. Sales pipeline'
      groupDescription: |-

        Moving an operation between stages:

            Draft → Tender → In Hand → In Progress (active operation)
                                  ↓
                                Lost

        These are separate endpoints rather than a `status` field on PATCH for two
        reasons. Each move has its own permission (`projects.move_to_active` is not
        `projects.edit`), and each has pre-conditions the caller cannot see —
        moving to Active needs both the consultant's acceptance email and the
        manager's approval on file.

        Every rule here lives in App\Services\SalesPipelineService, which the
        Filament panel calls too. A pre-condition failure arrives as
        **422 `business_rule_violated`** with a message written for a person: show
        it to the user directly rather than mapping it.
      subgroup: ''
      subgroupDescription: ''
      title: 'Move to Active'
      description: |-
        The last gate before manufacturing can start. Both signals must already
        be on the record: `acceptance_email_at` (set through PATCH /projects)
        and a manager approval (POST /manager-approve). The latest offer is
        marked as the winning one, the end date is filled in if Sales left it
        blank, and every department is notified.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      project_id:
        custom: []
        name: project_id
        description: 'The ID of the project.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      project:
        custom: []
        name: project
        description: 'The operation id.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      project_id: 1
      project: 1
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":{"id":1,"type":"project","status":{"value":"in_progress","label":"In Progress","color":"primary"}},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: Success
      -
        custom: []
        status: 422
        content: '{"error":{"code":"business_rule_violated","message":"Cannot move to Active: manager approval is required."},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: 'Manager approval missing'
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - POST
    uri: 'api/v1/projects/{project_id}/manager-approve'
    metadata:
      custom: []
      groupName: '12. Sales pipeline'
      groupDescription: |-

        Moving an operation between stages:

            Draft → Tender → In Hand → In Progress (active operation)
                                  ↓
                                Lost

        These are separate endpoints rather than a `status` field on PATCH for two
        reasons. Each move has its own permission (`projects.move_to_active` is not
        `projects.edit`), and each has pre-conditions the caller cannot see —
        moving to Active needs both the consultant's acceptance email and the
        manager's approval on file.

        Every rule here lives in App\Services\SalesPipelineService, which the
        Filament panel calls too. A pre-condition failure arrives as
        **422 `business_rule_violated`** with a message written for a person: show
        it to the user directly rather than mapping it.
      subgroup: ''
      subgroupDescription: ''
      title: 'Approve as manager'
      description: |-
        Records the signature that `move-to-active` checks for. Deliberately a
        separate step with its own permission: a Sales user fills in the
        acceptance date, a Sales manager signs off, then either may complete the
        move.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      project_id:
        custom: []
        name: project_id
        description: 'The ID of the project.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      project:
        custom: []
        name: project
        description: 'The operation id.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      project_id: 1
      project: 1
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":{"id":1,"type":"project","manager_approved_at":"2026-09-08T11:04:00+00:00"},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: Success
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - POST
    uri: 'api/v1/projects/{project_id}/cancel-to-lost'
    metadata:
      custom: []
      groupName: '12. Sales pipeline'
      groupDescription: |-

        Moving an operation between stages:

            Draft → Tender → In Hand → In Progress (active operation)
                                  ↓
                                Lost

        These are separate endpoints rather than a `status` field on PATCH for two
        reasons. Each move has its own permission (`projects.move_to_active` is not
        `projects.edit`), and each has pre-conditions the caller cannot see —
        moving to Active needs both the consultant's acceptance email and the
        manager's approval on file.

        Every rule here lives in App\Services\SalesPipelineService, which the
        Filament panel calls too. A pre-condition failure arrives as
        **422 `business_rule_violated`** with a message written for a person: show
        it to the user directly rather than mapping it.
      subgroup: ''
      subgroupDescription: ''
      title: 'Cancel to Lost'
      description: |-
        Terminal — there is no transition out of Lost. The reason and, where
        known, the competitor who won are captured for the year-end loss
        analysis, which is the whole point of recording it rather than deleting
        the operation.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      project_id:
        custom: []
        name: project_id
        description: 'The ID of the project.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      project:
        custom: []
        name: project
        description: 'The operation id.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      project_id: 1
      project: 1
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters:
      lost_reason:
        custom: []
        name: lost_reason
        description: 'One of the lost_reason enum values.'
        required: true
        example: high_price
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      lost_reason_note:
        custom: []
        name: lost_reason_note
        description: 'optional Free-text detail.'
        required: false
        example: '12% above the winning bid'
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: true
        deprecated: false
      winning_competitor:
        custom: []
        name: winning_competitor
        description: 'optional Who won it.'
        required: false
        example: Schneider
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: true
        deprecated: false
    cleanBodyParameters:
      lost_reason: high_price
      lost_reason_note: '12% above the winning bid'
      winning_competitor: Schneider
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":{"id":1,"type":"project","status":{"value":"lost","label":"Lost","color":"danger"},"winning_competitor":"Schneider"},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: Success
      -
        custom: []
        status: 422
        content: '{"error":{"code":"business_rule_violated","message":"Illegal transition: project is ''in_progress'', expected one of [tender, in_hand]."},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: 'Not in a cancellable stage'
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - PUT
    uri: 'api/v1/projects/{project_id}/alarm'
    metadata:
      custom: []
      groupName: '12. Sales pipeline'
      groupDescription: |-

        Moving an operation between stages:

            Draft → Tender → In Hand → In Progress (active operation)
                                  ↓
                                Lost

        These are separate endpoints rather than a `status` field on PATCH for two
        reasons. Each move has its own permission (`projects.move_to_active` is not
        `projects.edit`), and each has pre-conditions the caller cannot see —
        moving to Active needs both the consultant's acceptance email and the
        manager's approval on file.

        Every rule here lives in App\Services\SalesPipelineService, which the
        Filament panel calls too. A pre-condition failure arrives as
        **422 `business_rule_violated`** with a message written for a person: show
        it to the user directly rather than mapping it.
      subgroup: ''
      subgroupDescription: ''
      title: 'Set a reminder'
      description: |-
        The bell Sales sets on an operation it is chasing. Sending this again
        replaces the existing reminder.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      project_id:
        custom: []
        name: project_id
        description: 'The ID of the project.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      project:
        custom: []
        name: project
        description: 'The operation id.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      project_id: 1
      project: 1
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters:
      alarm_at:
        custom: []
        name: alarm_at
        description: 'ISO-8601 timestamp, in the future.'
        required: true
        example: '2026-09-20T09:00:00Z'
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      alarm_note:
        custom: []
        name: alarm_note
        description: 'optional What to chase.'
        required: false
        example: 'Call the consultant about the revised BOQ'
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: true
        deprecated: false
    cleanBodyParameters:
      alarm_at: '2026-09-20T09:00:00Z'
      alarm_note: 'Call the consultant about the revised BOQ'
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":{"id":1,"type":"project","alarm_at":"2026-09-20T09:00:00+00:00","alarm_note":"Call the consultant"},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: Success
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - DELETE
    uri: 'api/v1/projects/{project_id}/alarm'
    metadata:
      custom: []
      groupName: '12. Sales pipeline'
      groupDescription: |-

        Moving an operation between stages:

            Draft → Tender → In Hand → In Progress (active operation)
                                  ↓
                                Lost

        These are separate endpoints rather than a `status` field on PATCH for two
        reasons. Each move has its own permission (`projects.move_to_active` is not
        `projects.edit`), and each has pre-conditions the caller cannot see —
        moving to Active needs both the consultant's acceptance email and the
        manager's approval on file.

        Every rule here lives in App\Services\SalesPipelineService, which the
        Filament panel calls too. A pre-condition failure arrives as
        **422 `business_rule_violated`** with a message written for a person: show
        it to the user directly rather than mapping it.
      subgroup: ''
      subgroupDescription: ''
      title: 'Clear the reminder'
      description: ''
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      project_id:
        custom: []
        name: project_id
        description: 'The ID of the project.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      project:
        custom: []
        name: project
        description: 'The operation id.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      project_id: 1
      project: 1
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":{"id":1,"type":"project","alarm_at":null,"alarm_note":null},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: Success
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
