name: '38. Printable documents'
description: |-

  The printable PDFs — an offer, a purchase order, a quality certificate, the
  daybook, the ledger, the financial statements, a work order's material
  variance.

  Every endpoint here **streams `application/pdf`, not the JSON envelope.**
  That is the one place in the API where the response is not an envelope, and
  it is deliberate: a client that wants a document wants the bytes to hand to
  the platform's viewer or share sheet, not a base64 blob inside JSON that it
  would have to decode and write to a file first.

  These delegate to the **same controllers the admin panel prints through**,
  so the paper coming out of a phone is byte-for-byte the paper coming out of
  a desktop. Re-implementing the layouts here would guarantee the two drift,
  and the printed offer a customer holds is not a thing to let drift.

  Each document keeps its own print permission (`project_offers.print`,
  `purchase_orders.print`, `quality_sheets.print`, and the report permissions
  for the finance documents), enforced inside the shared controller. Being
  able to read a record through the API is not the same as being able to print
  it.

  **Language**: pass `?lang=ar` or `?lang=en` to choose the document's
  language independently of the caller's UI locale — the customer's copy of an
  offer is not always in the language of whoever pressed print. An unknown or
  missing value falls back to each document's own default.

  They sit on the reports rate limiter: rendering a PDF is the most expensive
  thing the API does.
endpoints:
  -
    custom: []
    httpMethods:
      - GET
    uri: 'api/v1/documents/offers/{offer_id}'
    metadata:
      custom: []
      groupName: '38. Printable documents'
      groupDescription: |-

        The printable PDFs — an offer, a purchase order, a quality certificate, the
        daybook, the ledger, the financial statements, a work order's material
        variance.

        Every endpoint here **streams `application/pdf`, not the JSON envelope.**
        That is the one place in the API where the response is not an envelope, and
        it is deliberate: a client that wants a document wants the bytes to hand to
        the platform's viewer or share sheet, not a base64 blob inside JSON that it
        would have to decode and write to a file first.

        These delegate to the **same controllers the admin panel prints through**,
        so the paper coming out of a phone is byte-for-byte the paper coming out of
        a desktop. Re-implementing the layouts here would guarantee the two drift,
        and the printed offer a customer holds is not a thing to let drift.

        Each document keeps its own print permission (`project_offers.print`,
        `purchase_orders.print`, `quality_sheets.print`, and the report permissions
        for the finance documents), enforced inside the shared controller. Being
        able to read a record through the API is not the same as being able to print
        it.

        **Language**: pass `?lang=ar` or `?lang=en` to choose the document's
        language independently of the caller's UI locale — the customer's copy of an
        offer is not always in the language of whoever pressed print. An unknown or
        missing value falls back to each document's own default.

        They sit on the reports rate limiter: rendering a PDF is the most expensive
        thing the API does.
      subgroup: ''
      subgroupDescription: ''
      title: 'Offer PDF'
      description: |-
        عرض السعر — the printable quotation, watermarked, in Arabic or English.
        Defaults to English, which is what offers have defaulted to since 2026.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      offer_id:
        custom: []
        name: offer_id
        description: 'The ID of the offer.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      offer:
        custom: []
        name: offer
        description: 'The offer id.'
        required: true
        example: 25
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      offer_id: 1
      offer: 25
    queryParameters:
      lang:
        custom: []
        name: lang
        description: 'ar or en.'
        required: false
        example: ar
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanQueryParameters:
      lang: ar
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '"<binary PDF>"'
        headers: []
        description: Success
      -
        custom: []
        status: 403
        content: '{"error":{"code":"forbidden","message":"You do not have permission to perform this action."},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: 'No print permission'
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: 'api/v1/documents/purchase-orders/{purchase_order_id}'
    metadata:
      custom: []
      groupName: '38. Printable documents'
      groupDescription: |-

        The printable PDFs — an offer, a purchase order, a quality certificate, the
        daybook, the ledger, the financial statements, a work order's material
        variance.

        Every endpoint here **streams `application/pdf`, not the JSON envelope.**
        That is the one place in the API where the response is not an envelope, and
        it is deliberate: a client that wants a document wants the bytes to hand to
        the platform's viewer or share sheet, not a base64 blob inside JSON that it
        would have to decode and write to a file first.

        These delegate to the **same controllers the admin panel prints through**,
        so the paper coming out of a phone is byte-for-byte the paper coming out of
        a desktop. Re-implementing the layouts here would guarantee the two drift,
        and the printed offer a customer holds is not a thing to let drift.

        Each document keeps its own print permission (`project_offers.print`,
        `purchase_orders.print`, `quality_sheets.print`, and the report permissions
        for the finance documents), enforced inside the shared controller. Being
        able to read a record through the API is not the same as being able to print
        it.

        **Language**: pass `?lang=ar` or `?lang=en` to choose the document's
        language independently of the caller's UI locale — the customer's copy of an
        offer is not always in the language of whoever pressed print. An unknown or
        missing value falls back to each document's own default.

        They sit on the reports rate limiter: rendering a PDF is the most expensive
        thing the API does.
      subgroup: ''
      subgroupDescription: ''
      title: 'Purchase order PDF'
      description: 'أمر التوريد — the order as sent to the supplier.'
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      purchase_order_id:
        custom: []
        name: purchase_order_id
        description: 'The ID of the purchase order.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      purchase_order:
        custom: []
        name: purchase_order
        description: 'The order id.'
        required: true
        example: 14
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      purchase_order_id: 1
      purchase_order: 14
    queryParameters:
      lang:
        custom: []
        name: lang
        description: 'ar or en.'
        required: false
        example: ar
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanQueryParameters:
      lang: ar
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '"<binary PDF>"'
        headers: []
        description: Success
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: 'api/v1/documents/quality-sheets/{quality_sheet_id}'
    metadata:
      custom: []
      groupName: '38. Printable documents'
      groupDescription: |-

        The printable PDFs — an offer, a purchase order, a quality certificate, the
        daybook, the ledger, the financial statements, a work order's material
        variance.

        Every endpoint here **streams `application/pdf`, not the JSON envelope.**
        That is the one place in the API where the response is not an envelope, and
        it is deliberate: a client that wants a document wants the bytes to hand to
        the platform's viewer or share sheet, not a base64 blob inside JSON that it
        would have to decode and write to a file first.

        These delegate to the **same controllers the admin panel prints through**,
        so the paper coming out of a phone is byte-for-byte the paper coming out of
        a desktop. Re-implementing the layouts here would guarantee the two drift,
        and the printed offer a customer holds is not a thing to let drift.

        Each document keeps its own print permission (`project_offers.print`,
        `purchase_orders.print`, `quality_sheets.print`, and the report permissions
        for the finance documents), enforced inside the shared controller. Being
        able to read a record through the API is not the same as being able to print
        it.

        **Language**: pass `?lang=ar` or `?lang=en` to choose the document's
        language independently of the caller's UI locale — the customer's copy of an
        offer is not always in the language of whoever pressed print. An unknown or
        missing value falls back to each document's own default.

        They sit on the reports rate limiter: rendering a PDF is the most expensive
        thing the API does.
      subgroup: ''
      subgroupDescription: ''
      title: 'Quality certificate PDF'
      description: |-
        ورقة الجودة — the inspection form with its test grid and both
        signatures, laid out as the paper version.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      quality_sheet_id:
        custom: []
        name: quality_sheet_id
        description: 'The ID of the quality sheet.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      quality_sheet:
        custom: []
        name: quality_sheet
        description: 'The sheet id.'
        required: true
        example: 12
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      quality_sheet_id: 1
      quality_sheet: 12
    queryParameters:
      lang:
        custom: []
        name: lang
        description: 'ar or en.'
        required: false
        example: ar
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanQueryParameters:
      lang: ar
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '"<binary PDF>"'
        headers: []
        description: Success
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: 'api/v1/documents/work-orders/{work_order_id}/material-variance'
    metadata:
      custom: []
      groupName: '38. Printable documents'
      groupDescription: |-

        The printable PDFs — an offer, a purchase order, a quality certificate, the
        daybook, the ledger, the financial statements, a work order's material
        variance.

        Every endpoint here **streams `application/pdf`, not the JSON envelope.**
        That is the one place in the API where the response is not an envelope, and
        it is deliberate: a client that wants a document wants the bytes to hand to
        the platform's viewer or share sheet, not a base64 blob inside JSON that it
        would have to decode and write to a file first.

        These delegate to the **same controllers the admin panel prints through**,
        so the paper coming out of a phone is byte-for-byte the paper coming out of
        a desktop. Re-implementing the layouts here would guarantee the two drift,
        and the printed offer a customer holds is not a thing to let drift.

        Each document keeps its own print permission (`project_offers.print`,
        `purchase_orders.print`, `quality_sheets.print`, and the report permissions
        for the finance documents), enforced inside the shared controller. Being
        able to read a record through the API is not the same as being able to print
        it.

        **Language**: pass `?lang=ar` or `?lang=en` to choose the document's
        language independently of the caller's UI locale — the customer's copy of an
        offer is not always in the language of whoever pressed print. An unknown or
        missing value falls back to each document's own default.

        They sit on the reports rate limiter: rendering a PDF is the most expensive
        thing the API does.
      subgroup: ''
      subgroupDescription: ''
      title: 'Work-order material variance PDF'
      description: |-
        فروق خامات أمر التصنيع — planned against issued, per material, in
        quantity and in money.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      work_order_id:
        custom: []
        name: work_order_id
        description: 'The ID of the work order.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      work_order:
        custom: []
        name: work_order
        description: 'The order id.'
        required: true
        example: 31
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      work_order_id: 1
      work_order: 31
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '"<binary PDF>"'
        headers: []
        description: Success
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/documents/journal-daybook
    metadata:
      custom: []
      groupName: '38. Printable documents'
      groupDescription: |-

        The printable PDFs — an offer, a purchase order, a quality certificate, the
        daybook, the ledger, the financial statements, a work order's material
        variance.

        Every endpoint here **streams `application/pdf`, not the JSON envelope.**
        That is the one place in the API where the response is not an envelope, and
        it is deliberate: a client that wants a document wants the bytes to hand to
        the platform's viewer or share sheet, not a base64 blob inside JSON that it
        would have to decode and write to a file first.

        These delegate to the **same controllers the admin panel prints through**,
        so the paper coming out of a phone is byte-for-byte the paper coming out of
        a desktop. Re-implementing the layouts here would guarantee the two drift,
        and the printed offer a customer holds is not a thing to let drift.

        Each document keeps its own print permission (`project_offers.print`,
        `purchase_orders.print`, `quality_sheets.print`, and the report permissions
        for the finance documents), enforced inside the shared controller. Being
        able to read a record through the API is not the same as being able to print
        it.

        **Language**: pass `?lang=ar` or `?lang=en` to choose the document's
        language independently of the caller's UI locale — the customer's copy of an
        offer is not always in the language of whoever pressed print. An unknown or
        missing value falls back to each document's own default.

        They sit on the reports rate limiter: rendering a PDF is the most expensive
        thing the API does.
      subgroup: ''
      subgroupDescription: ''
      title: 'Journal daybook PDF'
      description: |-
        دفتر اليومية التحليلي — the analytical daybook for a period, entries as
        rows against accounts as columns. Takes the same query parameters as the
        JSON endpoint.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters:
      from:
        custom: []
        name: from
        description: 'date Inclusive.'
        required: false
        example: '2026-01-01'
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      to:
        custom: []
        name: to
        description: 'date Inclusive.'
        required: false
        example: '2026-01-31'
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      account_ids:
        custom: []
        name: account_ids
        description: 'Comma-separated account ids to pin the columns to.'
        required: false
        example: '12,31'
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanQueryParameters:
      from: '2026-01-01'
      to: '2026-01-31'
      account_ids: '12,31'
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '"<binary PDF>"'
        headers: []
        description: Success
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/documents/general-ledger
    metadata:
      custom: []
      groupName: '38. Printable documents'
      groupDescription: |-

        The printable PDFs — an offer, a purchase order, a quality certificate, the
        daybook, the ledger, the financial statements, a work order's material
        variance.

        Every endpoint here **streams `application/pdf`, not the JSON envelope.**
        That is the one place in the API where the response is not an envelope, and
        it is deliberate: a client that wants a document wants the bytes to hand to
        the platform's viewer or share sheet, not a base64 blob inside JSON that it
        would have to decode and write to a file first.

        These delegate to the **same controllers the admin panel prints through**,
        so the paper coming out of a phone is byte-for-byte the paper coming out of
        a desktop. Re-implementing the layouts here would guarantee the two drift,
        and the printed offer a customer holds is not a thing to let drift.

        Each document keeps its own print permission (`project_offers.print`,
        `purchase_orders.print`, `quality_sheets.print`, and the report permissions
        for the finance documents), enforced inside the shared controller. Being
        able to read a record through the API is not the same as being able to print
        it.

        **Language**: pass `?lang=ar` or `?lang=en` to choose the document's
        language independently of the caller's UI locale — the customer's copy of an
        offer is not always in the language of whoever pressed print. An unknown or
        missing value falls back to each document's own default.

        They sit on the reports rate limiter: rendering a PDF is the most expensive
        thing the API does.
      subgroup: ''
      subgroupDescription: ''
      title: 'General ledger PDF'
      description: |-
        دفتر الأستاذ — one account's movements for a period, with its opening
        and closing balances.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters:
      account_id:
        custom: []
        name: account_id
        description: 'The account to print.'
        required: true
        example: 12
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      from:
        custom: []
        name: from
        description: 'date Inclusive.'
        required: false
        example: '2026-01-01'
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      to:
        custom: []
        name: to
        description: 'date Inclusive.'
        required: false
        example: '2026-06-30'
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanQueryParameters:
      account_id: 12
      from: '2026-01-01'
      to: '2026-06-30'
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '"<binary PDF>"'
        headers: []
        description: Success
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/documents/financial-statements
    metadata:
      custom: []
      groupName: '38. Printable documents'
      groupDescription: |-

        The printable PDFs — an offer, a purchase order, a quality certificate, the
        daybook, the ledger, the financial statements, a work order's material
        variance.

        Every endpoint here **streams `application/pdf`, not the JSON envelope.**
        That is the one place in the API where the response is not an envelope, and
        it is deliberate: a client that wants a document wants the bytes to hand to
        the platform's viewer or share sheet, not a base64 blob inside JSON that it
        would have to decode and write to a file first.

        These delegate to the **same controllers the admin panel prints through**,
        so the paper coming out of a phone is byte-for-byte the paper coming out of
        a desktop. Re-implementing the layouts here would guarantee the two drift,
        and the printed offer a customer holds is not a thing to let drift.

        Each document keeps its own print permission (`project_offers.print`,
        `purchase_orders.print`, `quality_sheets.print`, and the report permissions
        for the finance documents), enforced inside the shared controller. Being
        able to read a record through the API is not the same as being able to print
        it.

        **Language**: pass `?lang=ar` or `?lang=en` to choose the document's
        language independently of the caller's UI locale — the customer's copy of an
        offer is not always in the language of whoever pressed print. An unknown or
        missing value falls back to each document's own default.

        They sit on the reports rate limiter: rendering a PDF is the most expensive
        thing the API does.
      subgroup: ''
      subgroupDescription: ''
      title: 'Financial statements PDF'
      description: |-
        القوائم المالية — the statements that follow the trial balance, printed
        as one document.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters:
      from:
        custom: []
        name: from
        description: 'date Inclusive.'
        required: false
        example: '2026-01-01'
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      to:
        custom: []
        name: to
        description: 'date Inclusive.'
        required: false
        example: '2026-12-31'
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanQueryParameters:
      from: '2026-01-01'
      to: '2026-12-31'
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '"<binary PDF>"'
        headers: []
        description: Success
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
