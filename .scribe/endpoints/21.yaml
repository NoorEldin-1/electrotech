name: '24. Production entries'
description: |-

  الإنتاج والفاقد — what a completed work order actually produced, and what it
  lost doing so. One row per finished product per completed order.

  **Read-only, deliberately.** `ProductionEntryPolicy` answers false to
  create, update and delete, and this controller has no write endpoints to
  match. These rows are written by the work order's `complete` transition, at
  the same moment the stock moves. A writable production entry would let the
  loss figures be edited away from the movements that produced them, with
  nothing left to reconcile against.

  `costs.loss_value` is the money the scrap cost — scrap × the actual cost per
  produced unit — not the quantity. It is the figure the value-based loss
  report aggregates, which is why it is published here rather than left to
  each client to re-derive.
endpoints:
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/production-entries
    metadata:
      custom: []
      groupName: '24. Production entries'
      groupDescription: |-

        الإنتاج والفاقد — what a completed work order actually produced, and what it
        lost doing so. One row per finished product per completed order.

        **Read-only, deliberately.** `ProductionEntryPolicy` answers false to
        create, update and delete, and this controller has no write endpoints to
        match. These rows are written by the work order's `complete` transition, at
        the same moment the stock moves. A writable production entry would let the
        loss figures be edited away from the movements that produced them, with
        nothing left to reconcile against.

        `costs.loss_value` is the money the scrap cost — scrap × the actual cost per
        produced unit — not the quantity. It is the figure the value-based loss
        report aggregates, which is why it is published here rather than left to
        each client to re-derive.
      subgroup: ''
      subgroupDescription: ''
      title: 'List production entries'
      description: ''
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters:
      page:
        custom: []
        name: page
        description: 'The page number.'
        required: false
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      per_page:
        custom: []
        name: per_page
        description: 'Rows per page, max 100.'
        required: false
        example: 25
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      search:
        custom: []
        name: search
        description: 'Matches the operation name.'
        required: false
        example: 'Main distribution'
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      'filter[work_order]':
        custom: []
        name: 'filter[work_order]'
        description: 'Work order id.'
        required: false
        example: 31
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      'filter[output_item]':
        custom: []
        name: 'filter[output_item]'
        description: 'Finished-product item id.'
        required: false
        example: 21
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      'filter[entry_date]':
        custom: []
        name: 'filter[entry_date]'
        description: 'Inclusive date range from,until.'
        required: false
        example: '2026-01-01,2026-06-30'
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      sort:
        custom: []
        name: sort
        description: 'Allowed: entry_date, produced_quantity, scrap_quantity, created_at.'
        required: false
        example: '-entry_date'
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      include:
        custom: []
        name: include
        description: 'Allowed: workOrder, outputItem.'
        required: false
        example: outputItem
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanQueryParameters:
      page: 1
      per_page: 25
      search: 'Main distribution'
      'filter[work_order]': 31
      'filter[output_item]': 21
      'filter[entry_date]': '2026-01-01,2026-06-30'
      sort: '-entry_date'
      include: outputItem
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":[{"id":77,"type":"production_entry","work_order_id":31,"output_item_id":21,"quantities":{"planned":"10.0000","produced":"9.0000","scrap":"1.0000","scrap_percentage":"10.00"},"costs":{"actual_material":"43000.00","loss_value":"4777.78"}}],"meta":{"request_id":"9f1c","api_version":"1","pagination":{"total":1,"count":1,"per_page":25,"current_page":1,"total_pages":1}},"links":{"first":"","prev":null,"next":null,"last":""}}'
        headers: []
        description: Success
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: 'api/v1/production-entries/{production_entry_id}'
    metadata:
      custom: []
      groupName: '24. Production entries'
      groupDescription: |-

        الإنتاج والفاقد — what a completed work order actually produced, and what it
        lost doing so. One row per finished product per completed order.

        **Read-only, deliberately.** `ProductionEntryPolicy` answers false to
        create, update and delete, and this controller has no write endpoints to
        match. These rows are written by the work order's `complete` transition, at
        the same moment the stock moves. A writable production entry would let the
        loss figures be edited away from the movements that produced them, with
        nothing left to reconcile against.

        `costs.loss_value` is the money the scrap cost — scrap × the actual cost per
        produced unit — not the quantity. It is the figure the value-based loss
        report aggregates, which is why it is published here rather than left to
        each client to re-derive.
      subgroup: ''
      subgroupDescription: ''
      title: 'Show a production entry'
      description: ''
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {ACCESS_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      production_entry_id:
        custom: []
        name: production_entry_id
        description: 'The ID of the production entry.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      production_entry:
        custom: []
        name: production_entry
        description: 'The entry id.'
        required: true
        example: 77
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      production_entry_id: 1
      production_entry: 77
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"data":{"id":77,"type":"production_entry","work_order_id":31,"quantities":{"produced":"9.0000","scrap":"1.0000"},"costs":{"loss_value":"4777.78","loss_value_percentage":"11.11"}},"meta":{"request_id":"9f1c","api_version":"1"}}'
        headers: []
        description: Success
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 3V5EgbkvZcDPa166h8fd4ae'
    controller: null
    method: null
    route: null
