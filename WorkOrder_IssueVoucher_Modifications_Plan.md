# خطة تعديلات أمر التصنيع وإذن الصرف

**المصدر:** طلب العميل — تعديلات على شاشتَي «أوامر التصنيع» و«أذون الصرف».
**التاريخ:** 2026-08-04
**الحالة:** ✅ كل المراحل (١–٩) منفَّذة. 21 اختباراً جديداً، والمجموعة كلها خضراء
(535 ناجح) عدا 3 إخفاقات سابقة لا علاقة لها بهذا العمل
(`ActivityLogTest::subject_type_is_translated` — توقُّع قديم لاسم «Work Order» بعد إعادة
التسمية إلى «Manufacturing Order»، واختبارَي `NetworkResilienceTest` المرتبطَين بالبيئة).

---

## ١ · قراءة الطلب وتحليله

### أ — أمر التشغيل (`WorkOrderResource`)

| # | طلب العميل | القراءة الهندسية | الوضع الحالي |
|---|-----------|------------------|--------------|
| أ-١ | «قائمة المواد المرتبطة مش عايزه خالص» | حذف `Select::make('bom_id')` من الفورم | موجود، ويُستخدم كمصدر بديل للخامات + مصدر `estimated_cost` |
| أ-٢ | «المنتج التام يكون multi-select» | أمر التصنيع ينتج **أكثر من منتج تام** | `output_item_id` عمود مفرد `belongsTo` |
| أ-٣ | «الكمية المخططة تتحدّث مع المنتج التام — كمية لكل منتج — والأصناف المنصرفة تواكبها» | كمية مخططة **لكل منتج**، والإجمالي مشتق منها، وجدول الخامات = Σ (تركيبة كل منتج × كميته) | `planned_quantity` رقم واحد يُدخَل يدوياً |
| أ-٤ | «الكمية المنتجة وكمية الهالك تتشال من create/edit وتتحدد من مراحل الأمر» | الحقلان يخرجان من الفورم نهائياً ويُدخلان في مرحلة «إرسال لضمان الجودة» — **لكل منتج** | الحقلان في الفورم (معطّلان لكنهما ظاهران) ويُدخلان مجمّعَين في `submit_qa` |
| أ-٥ | «انتهاء التصنيع لازم قبله اعتماد مكتب المشروعات + ضمان الجودة، وتقييد شديد» | بوابة مزدوجة على `finishManufacturing()` — في الخدمة وفي ظهور الزر | الزر يظهر في `InProgress`/`QaReview` بدون أي اشتراط اعتماد |

### ب — إذن الصرف (`IssueVoucherResource`)

| # | طلب العميل | القراءة الهندسية | الوضع الحالي |
|---|-----------|------------------|--------------|
| ب-١ | «اختيار أمر التصنيع يملأ الأصناف المنصرفة تلقائياً بالكمية والسعر، بأداء عالٍ، وقابلة للتعديل بعدها» | `live()` على `work_order_id` + ملء `lines` من **المتبقّي المطلوب** = المخطط − (المنصرف المرحَّل − المرتد) | لا يوجد أي ربط؛ المستخدم يُدخل كل سطر يدوياً |
| ب-٢ | «لو الكمية زيادة عن اللي محتاجه أمر التصنيع، يتعرف قبل الترحيل، ويُفتح للمستخدم الرجوع والتعديل أو الاستكمال والموافقة — والجزء ده يدخل مع الصلاحيات» | كشف تجاوز قبل الترحيل + بوابة صلاحية `issue_vouchers.approve_excess` + سبب إلزامي مُسجَّل | `post()` يرحّل أي كمية دون أي مقارنة بالخطة |

### التفاصيل الساقطة التي أضفتها بحكم طبيعة الأنظمة الصناعية

1. **الكمية المخططة الإجمالية تصبح مشتقة** — لو بقيت قابلة للكتابة اليدوية سيتناقض المجموع مع سطور المنتجات، وكل نسب الأمر (الكفاءة، الفاقد، انحراف التكلفة) مقامها هذا الرقم.
2. **الحفاظ على `output_item_id`** كـ«المنتج الأساسي» (أول سطر) — عشرات المواضع تقرأه: ورقة الجودة، إشعار انتهاء التصنيع، PDF مقارنة الخامات، تقارير الإنتاج. حذفه = كسر عرضي واسع.
3. **قيد إنتاج (`ProductionEntry`) لكل منتج** عند الإتمام، وتوزيع تكلفة الخامات المخططة/الفعلية **بالتناسب مع الكمية المخططة** لكل منتج — حتى يظل إجمالي تقرير «الإنتاج والفاقد» مطابقاً تماماً لما كان.
4. **منتج بلا تركيبة قياسية لا يُسقِط العملية كلها** — تُجلب خامات باقي المنتجات ويُنبَّه المستخدم بالمنتجات التي لا تركيبة لها.
5. **احترام التعديل اليدوي** — إعادة الحساب التلقائي للخامات تتوقف بمجرد وجود سطر مُعدَّل يدوياً (`is_manual`)، ويبقى زر «جلب الخامات القياسية» للاستبدال الصريح بتأكيد.
6. **الملء التلقائي لإذن الصرف = المتبقّي وليس المخطط الكامل** — وإلا فإن إذن الصرف الثاني لنفس الأمر يقترح صرفاً مزدوجاً، وهو أكثر خطأ متكرر في أنظمة المخازن.
7. **تسجيل موافقة التجاوز على المستند** (`excess_reason` / `excess_approved_by` / `excess_approved_at`) — الموافقة الشفهية لا تصلح في مستند يحرّك مخزوناً وقيمة.
8. **تحذير حي أثناء الكتابة** في سطور الإذن قبل الوصول لزر الترحيل — أرخص وقت لاكتشاف الخطأ.
9. **`estimated_cost`** كان يُلتقط من الـ BOM المحذوف؛ يصبح مشتقاً من تكلفة خامات الأمر المخططة.

---

## ٢ · قرار التصميم في «multi-select»

طلب العميل حرفياً «select يكون multi-select»، لكنه في نفس الجملة طلب «كمية لكل منتج».
عنصر multi-select واحد لا يحمل كمية لكل قيمة. الحل المُنفَّذ هو **جدول المنتجات التامة**
(`Repeater` بسطر = منتج + كميته المخططة):

- يحقق الغرض: أكثر من منتج تام على الأمر الواحد.
- يحمل الكمية لكل منتج (وهو الطلب الثالث حرفياً).
- يمنع تكرار نفس المنتج، ويُظهر إجمالي الكمية المخططة حيّاً أسفله.

---

## ٣ · المراحل

### المرحلة ١ — قاعدة البيانات
- `work_order_outputs`: `work_order_id`, `item_id`, `planned_quantity`, `produced_quantity`, `waste_quantity`, `notes` + فهرس فريد `(work_order_id, item_id)`.
- ترحيل البيانات القائمة: كل أمر له `output_item_id` يُنشأ له سطر واحد بكميات الأمر.
- `issue_vouchers` += `has_excess`, `excess_reason`, `excess_approved_by`, `excess_approved_at`.

### المرحلة ٢ — النماذج
- `WorkOrderOutput` (جديد) + `WorkOrder::outputs()` + `syncDerivedPlan()`.
- `IssueVoucher` += الحقول الجديدة + `hasExcess()`.

### المرحلة ٣ — الخدمات
- `WorkOrderMaterialService::standardMaterialsForOutputs()` — دمج تركيبات كل المنتجات في جدول خامات واحد.
- `WorkOrderService`: بوابة انتهاء التصنيع، `submitForQa()` لكل منتج، `complete()` متعدد المنتجات، `assertPlanIsComplete()` يشترط منتجاً واحداً على الأقل.
- `IssueVoucherService`: `requirementFor()` / `suggestedLinesFor()` / `excessReport()` / `post($voucher, $allowExcess, $reason)` + `ExcessIssueException`.

### المرحلة ٤ — واجهة أمر التصنيع
حذف قائمة المواد المرتبطة · جدول المنتجات التامة · إجمالي مخطط محسوب · حذف المنتج/الهالك من الفورم · نموذج «إرسال لضمان الجودة» لكل منتج · بوابة انتهاء التصنيع في الظهور والخدمة · عمود المنتجات في الجدول.

### المرحلة ٥ — واجهة إذن الصرف
ملء تلقائي من أمر التصنيع · زر إعادة جلب · تلميح «المتبقي المطلوب» لكل سطر · تحذير حي عند التجاوز · نافذة ترحيل تعرض جدول التجاوز وتطلب سبباً · حجب الترحيل عمّن لا يملك صلاحية اعتماد التجاوز.

### المرحلة ٦ — الصلاحيات
`issue_vouchers.approve_excess` + سياسة `approveExcess` + إسناد للأدوار (مدير المخزن، مدير المصنع، المدير العام).

### المرحلة ٧ — الترجمة (ar/en)

### المرحلة ٨ — الاختبارات
`WorkOrderMultiOutputTest` · `ManufacturingFinishGateTest` · `IssueVoucherAutofillTest` · `IssueVoucherExcessTest` + تحديث الاختبارات المتأثرة.

### المرحلة ٩ — تحديث دليل الاستخدام
`documentation/partials/pmo.blade.php` و`warehouse.blade.php` و`manufacturing.blade.php`.
