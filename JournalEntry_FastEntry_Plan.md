# خطة تعديلات "قيود اليومية" — إدخال سريع + فصل المدين والدائن

> ✅ **الحالة: تم تنفيذ المراحل الأربعة بالكامل** (2026-07-28). الاختبارات: 69 اختبار في `tests/Feature/Finance` كلها خضراء،
> منها 22 اختبار جديد (`JournalEntryLinesTest` + `JournalEntryFormTest`). التغييرات غير مرفوعة (uncommitted) على `main`.

> جولة ملاحظات على شاشة **قيود اليومية** (الصور المرسلة: صفحة القائمة، صفحة الإضافة، قسم «سطور القيد»، وصفحة التوثيق).
> المرجع: قراءة الكود الفعلي (Laravel 12 + Filament 3.3) — `JournalEntryResource`, `JournalEntry`, `JournalEntryLine`, `JournalEntryService`.

---

## 1. تفكيك الطلب (ماذا يطلب العميل بالضبط)

| # | الطلب | التفصيل |
|---|---|---|
| ١ | **قائمة منسدلة عند «إضافة قيد»** | الضغط على الزر في صفحة القائمة يفتح Dropdown بأنواع المستند الثلاثة؛ اختيار نوع ⇐ الدخول لصفحة الإضافة و**نوع المستند محدد تلقائياً**. |
| ٢ | **حساب الخزينة تلقائي** | لو النوع **أمر صرف** أو **إيصال توريد**، الحساب يتحدد تلقائياً على **حساب الخزينة**. |
| ٣ | **فصل المدين عن الدائن** | بدل «النوع = مدين/دائن» داخل كل سطر: جانب **مدين** مستقل له قيمته، وجانب **دائن** مستقل له قيمته. |
| ٤ | **زر إضافة (+) لكل جانب** | لأن القيد قد يكون **متعدد الأطراف** (أكثر من مدين أو أكثر من دائن)، كل جانب له زر إضافة سطر جديد بحساب وقيمة. |
| ٥ | **تسهيل الكتابة لأقصى درجة** | ⭐ **الأهم** — الجزء ده بيتم التعامل معاه **يومياً وبكثرة**، فلازم أقل عدد نقرات وأقل تمرير (scroll) وأقل حقول ظاهرة. |
| ٦ | **تحديث التوثيق** | تحديث صفحة `/documentation` بكل التعديلات دي. |

---

## 2. الوضع الحالي في الكود (حقائق)

| البند | الحالة | الملف |
|---|---|---|
| زر الإضافة في القائمة | زر واحد `Actions\CreateAction` بدون خيارات | `JournalEntryResource/Pages/ListJournalEntries.php:18` |
| نوع المستند في الفورم | `Select` عادي بدون قيمة مبدئية ولا `live()` | `JournalEntryResource.php:78-81` |
| سطور القيد | **Repeater واحد** `lines` بـ 3 أعمدة، كل سطر فيه: الحساب + العملية + **النوع (مدين/دائن)** + القيمة + ملاحظات | `JournalEntryResource.php:121-169` |
| ارتفاع السطر الواحد | ~5 حقول موزعة على **3 صفوف** لكل سطر ⇒ سطران فقط يملآن الشاشة (واضح في الصورة الثالثة) | — |
| عدّاد التوازن | `Placeholder` أسفل السطور، بألوان **hex ثابتة** (`#059669` / `#e11d48`) ⇒ لا يحترم الوضع الليلي | `JournalEntryResource.php:171-206` |
| تخزين السطر | جدول واحد `journal_entry_lines` بعمود `direction` (enum مدين/دائن) | `app/Models/JournalEntryLine.php` |
| حساب الخزينة | موجود في شجرة الحسابات بكود **`1010` الخزينة** و **`1011` خزينة أجنبي (USD)** | `ChartOfAccountsSeeder.php:53-54` |
| منطق مشابه موجود بالفعل | `OperationPaymentService`: وارد ⇒ *مدين الخزينة* + `SupplyReceipt`، صادر ⇒ *دائن الخزينة* + `PaymentOrder` | `OperationPaymentService.php:119-122` |
| ما يقرأ السطور | `GeneralLedgerService`, `OperationCostService`, `LedgerEntriesRelationManager`, اليومية التحليلية | — |
| الاختبارات الحالية | اختبارات موديل/خدمة فقط (لا اختبارات Livewire للفورم) ⇒ **لن تنكسر** | `tests/Feature/Finance/JournalEntryTest.php` |

---

## 3. القرارات التصميمية (اتُّخذت هنا دون انتظار العميل)

1. **لا تغيير في قاعدة البيانات إطلاقاً.** الفصل بين المدين والدائن **في الواجهة فقط**؛ التخزين يبقى جدول `journal_entry_lines` بعمود `direction`. السبب: دفتر الأستاذ واليومية التحليلية وميزان المراجعة ومركز تكلفة العملية وإقفال COGS كلها تقرأ من `direction` — أي تغيير في المخطط يعني كسر خمس شاشات مالية بلا مقابل. ⇒ **صفر Migration، صفر خطر على البيانات القائمة.**

2. **ربط الفورم بالجدول يدوياً وليس عبر `->relationship()`.** سنستخدم Repeaterين عاديين (`debit_lines` / `credit_lines`) مع خدمة صريحة `JournalEntryService::splitLines()` / `syncLines()`. السبب: ربط Repeaterين بعلاقة `hasMany` واحدة مقيّدة يعتمد على سلوك داخلي في Filament للحذف — لو تصرّف غير المتوقع فالنتيجة **حذف الجانب الآخر بالكامل**. الطريقة اليدوية صريحة وقابلة للاختبار.

3. **الخزينة على أي جانب؟** مطابقة لمنطق `OperationPaymentService` القائم:
   - **أمر صرف** (فلوس خارجة) ⇒ الخزينة في الجانب **الدائن**، والمصروف/المورد في المدين.
   - **إيصال توريد** (فلوس داخلة) ⇒ الخزينة في الجانب **المدين**، والعميل/الإيراد في الدائن.
   - **قيد تسوية** ⇒ لا تعبئة تلقائية.

4. **الحساب المُعبَّأ يبقى قابلاً للتعديل** (مش مقفول): أحياناً الصرف يكون من بنك مش من الخزينة. + التعبئة **لا تكتب فوق اختيار المستخدم** أبداً (تعمل فقط لو الحقل فاضي).

5. **كود الخزينة قابل للضبط** في `config/finance.php` بدل التثبيت في الكود: `treasury_account_code = '1010'` مع خريطة لكل عملة (`USD => 1011`). ولو الكود غير موجود في الشجرة ⇒ **تخطي صامت** (لا يفشل الفورم على نظام بشجرة حسابات مخصّصة).

6. **سطر واحد = صف واحد بصرياً.** كل سطر يعرض **حقلين فقط**: الحساب + القيمة. أما **العملية (مركز التكلفة)** و**ملاحظات السطر** فتُخفى خلف مفتاح واحد «إظهار مركز التكلفة والملاحظات»، مع `->dehydratedWhenHidden()` حتى **لا تضيع قيم محفوظة**، وتظهر تلقائياً لو السطر يحمل قيمة فيها بالفعل. هذا وحده يقلّص ارتفاع السطر من **3 صفوف إلى صف واحد** (~‏٦٥٪ أقل تمرير).

7. **الجانبان جنباً إلى جنب** (مدين يمين / دائن يسار في RTL) في شبكة عمودين، فيُرى القيد كله في شاشة واحدة كما يُقرأ في الدفتر الورقي — وينهار لعمود واحد على الموبايل.

8. **التعبئة التلقائية للفرق المتبقي:** عند إضافة سطر جديد، تُملأ خانة القيمة تلقائياً بـ **الفرق المتبقي** بين الجانبين (لو موجب). مثال: مدين ١٢٬٠٠٠ ⇒ الضغط على «إضافة سطر دائن» يعطيك سطراً بقيمة ١٢٬٠٠٠ جاهزة ⇒ **القيد المتوازن العادي يتم بنقرة واحدة وكتابة رقم واحد**. + زر آلة حاسبة صغير داخل كل خانة قيمة يملأ الفرق يدوياً في أي وقت.

9. **المسودة غير المتوازنة تبقى قابلة للحفظ** (المحاسب قد يحفظ عملاً ناقصاً)، والمنع يبقى عند **الترحيل** فقط كما هو الآن. لكن عدّاد التوازن يصبح **أعلى القسم** لا أسفله، وبألوان Filament الدلالية (تعمل في الوضعين الفاتح والداكن).

10. **`رقم القيد` و`مرجع القيد`** يُخفيان في صفحة الإضافة (`hiddenOn('create')`) لأنهما دائماً فارغان وقتها ويأخذان صفاً كاملاً — ويظهران في صفحة التعديل/العرض. توفير إضافي في الارتفاع.

11. **لا صلاحيات جديدة.** عناصر القائمة المنسدلة ترث `journal_entries.create`.

---

## 4. شكل الشاشة بعد التعديل

**صفحة القائمة:**
```
┌──────────────────────────────┐
│  ▾ إضافة قيد يومية           │   ← Dropdown
├──────────────────────────────┤
│ ⚫ أمر صرف                    │ → /create?document_type=payment_order
│ 🔴 إيصال توريد                │ → /create?document_type=supply_receipt
│ 🟢 قيد تسوية                  │ → /create?document_type=settlement
└──────────────────────────────┘
```

**صفحة الإضافة — قسم السطور:**
```
┌───────────────────────── سطور القيد ─────────────────────────┐
│  مدين ١٢٬٠٠٠٫٠٠   |   دائن ١٢٬٠٠٠٫٠٠   |   ✓ متوازن          │  ← شريط حي أعلى القسم
│  [ ] إظهار مركز التكلفة وملاحظات السطر                        │
├───────────────── مدين ──────────────┬───────── دائن ─────────┤
│ [الحساب ▾           ][ ١٢٬٠٠٠ 🧮][🗑]│[الخزينة ▾ ][١٢٬٠٠٠ 🧮][🗑]│  ← صف واحد للسطر
│ [الحساب ▾           ][       0 🧮][🗑]│                        │
│          + إضافة سطر مدين           │    + إضافة سطر دائن     │
└─────────────────────────────────────┴────────────────────────┘
```

---

## 5. خطة التنفيذ بالمراحل

### المرحلة ١ — الأساس: الإعدادات + خدمة التحويل (بدون أي تغيير مرئي)

| الملف | العمل |
|---|---|
| `config/finance.php` | **جديد**: `treasury_account_code = '1010'`، `treasury_account_codes_by_currency = ['USD' => '1011']`، مع تعليق توضيحي بنفس أسلوب `config/operations.php`. |
| `app/Services/JournalEntryService.php` | **إضافة 3 دوال**: |

```php
/** يقسّم سطور القيد إلى مصفوفتين لتعبئة الفورم. */
public static function splitLines(JournalEntry $entry): array
// => ['debit_lines' => [['id'=>..,'account_id'=>..,'project_id'=>..,'amount'=>..,'line_notes'=>..], ...],
//     'credit_lines' => [...]]

/** يكتب الجانبين في journal_entry_lines: تحديث بالـ id، إنشاء للجديد، حذف للمحذوف — داخل transaction. */
public function syncLines(JournalEntry $entry, array $debitLines, array $creditLines): void

/** يرجّع حساب الخزينة المناسب لنوع المستند والعملة، أو null. */
public static function treasuryAccountFor(?DocumentType $type, ?string $currency): ?array
// => ['direction' => AccountDirection::Credit, 'account_id' => 12]  لأمر الصرف
```

- `syncLines` يستدعي `recalculateTotals()` في نهايته، ويرفض العمل على قيد **مُرحَّل** (حماية إضافية).
- `treasuryAccountFor` تبحث بالكود، وتعيد `null` بصمت لو الحساب غير موجود أو غير نشط أو النوع = تسوية.

**اختبارات المرحلة** (`tests/Feature/Finance/JournalEntryLinesTest.php`):
- `splitLines` يفصل الاتجاهين ويحافظ على `id` و`project_id` والقيم.
- `syncLines` ينشئ / يعدّل / يحذف بشكل صحيح ويضبط `direction` ويعيد حساب الإجماليات.
- `syncLines` يرفض قيداً مُرحَّلاً.
- `treasuryAccountFor`: أمر صرف ⇒ دائن، إيصال توريد ⇒ مدين، تسوية ⇒ null، عملة USD ⇒ `1011`، كود مفقود ⇒ null.

---

### المرحلة ٢ — ⭐ إعادة بناء «سطور القيد» (جوهر الطلب)

`app/Filament/Resources/JournalEntryResource.php`:

1. **حذف** الـ Repeater الحالي `lines` وحقل `direction` منه.
2. **قسم جديد «سطور القيد»** يحتوي بالترتيب:
   - `Placeholder` **شريط التوازن** (أعلى القسم): إجمالي مدين | إجمالي دائن | الفرق أو «✓ متوازن»، بفئات Filament الدلالية (`text-success-600 dark:text-success-400` / `text-danger-…`) بدل الـ hex الحالي ⇒ يعمل في الوضع الداكن (التزاماً بقواعد `dark-theme-tokens`).
   - `Toggle::make('show_line_details')` → `->live()->dehydrated(false)` «إظهار مركز التكلفة وملاحظات السطر».
   - `Grid::make(2)` فيها Repeaterان:

```php
Forms\Components\Repeater::make('debit_lines')   // ونظيره credit_lines
    ->label(__('…sections.debit_lines'))          // «مدين» / «دائن»
    ->addActionLabel(__('…actions.add_debit_line'))
    ->defaultItems(1)->minItems(1)
    ->reorderable(false)        // لا مقبض سحب ⇒ صف أضيق
    ->cloneable()               // تكرار سطر متشابه بنقرة
    ->columns(12)
    ->schema([
        Hidden::make('id'),
        Select::make('account_id')…->columnSpan(7),
        TextInput::make('amount')->numeric()->minValue(0.01)
            ->live(onBlur: true)
            ->default(fn (Get $get) => static::remainingFor('debit', $get))   // ← تعبئة الفرق
            ->suffix(fn (Get $get) => $get('../../currency'))
            ->suffixAction(Action::make('fillRemaining')->icon('heroicon-m-calculator')…)
            ->columnSpan(5),
        Select::make('project_id')…->columnSpanFull()
            ->visible(fn (Get $get, $state) => $get('../../show_line_details') || filled($state))
            ->dehydratedWhenHidden(),
        TextInput::make('line_notes')…            // نفس شرط الظهور
    ])
    ->disabled(fn (?JournalEntry $r) => $r?->isPosted() ?? false)
```

3. **حقول الرأس**: `entry_serial` و`entry_number` ⇐ `->hiddenOn('create')`؛ `document_type` ⇐ `->live()` + `afterStateUpdated` لتطبيق الخزينة؛ `currency` ⇐ `->live()` (لتحديث لاحقة القيمة وحساب الخزينة بالعملة).
4. **دالة مساعدة** `applyTreasuryDefault(Get $get, Set $set)` في الـ Resource: تستدعي `treasuryAccountFor()` وتضع الحساب في **أول سطر فارغ** من الجانب الصحيح فقط.

**ربط الصفحات:**

| الملف | العمل |
|---|---|
| `Pages/CreateJournalEntry.php` | `mutateFormDataBeforeCreate`: نزع `debit_lines`/`credit_lines`/`show_line_details` من البيانات وحفظها في خاصية مؤقتة. `afterCreate`: `syncLines(...)`. |
| `Pages/EditJournalEntry.php` | `mutateFormDataBeforeFill`: دمج `splitLines($record)`. `mutateFormDataBeforeSave`: نزع المفاتيح. `afterSave`: `syncLines(...)`. |
| `JournalEntryResource::table()` | `ViewAction::make()->mutateRecordDataUsing(fn ($data, $record) => [...$data, ...JournalEntryService::splitLines($record)])` — وإلا ظهرت السطور فارغة في نافذة العرض. |

**اختبارات المرحلة** (`tests/Feature/Finance/JournalEntryFormTest.php` — اختبارات Livewire):
- إنشاء قيد بجانبين ⇒ سطران بالاتجاهين الصحيحين والإجماليات مضبوطة.
- قيد متعدد الأطراف (٢ مدين + ١ دائن) ⇒ ٣ سطور صحيحة.
- تعديل قيد قائم: حذف سطر + تعديل قيمة + إضافة سطر ⇒ النتيجة النهائية مطابقة بلا سطور يتيمة.
- `project_id` المخفي لا يضيع بعد الحفظ (اختبار `dehydratedWhenHidden`).

---

### المرحلة ٣ — القائمة المنسدلة + التحديد التلقائي للنوع

| الملف | العمل |
|---|---|
| `Pages/ListJournalEntries.php` | استبدال `CreateAction` بـ `Actions\ActionGroup::make([...])->label('إضافة قيد يومية')->icon('heroicon-o-plus')->button()->color('primary')`، بداخلها ٣ إجراءات — واحد لكل `DocumentType` — بلون النوع وأيقونته، و`->url(JournalEntryResource::getUrl('create', ['document_type' => $type->value]))`. تظهر فقط بصلاحية الإنشاء. |
| `JournalEntryResource.php` | `document_type` ⇐ `->default(fn () => DocumentType::tryFrom((string) request()->query('document_type'))?->value)` — يعمل في الإضافة فقط ويُتجاهل في التعديل. |
| `Pages/CreateJournalEntry.php` | بعد التعبئة الأولى: تطبيق حساب الخزينة على الجانب الصحيح حسب النوع القادم من الـ URL (`afterFill`). |

**اختبارات المرحلة:**
- زيارة `/admin/journal-entries/create?document_type=payment_order` ⇒ النوع محدد، والجانب **الدائن** فيه حساب الخزينة `1010`.
- `?document_type=supply_receipt` ⇒ الخزينة في الجانب **المدين**.
- `?document_type=settlement` ⇒ لا تعبئة.
- قيمة `document_type` غير صالحة في الـ URL ⇒ لا انهيار، الحقل فارغ.

---

### المرحلة ٤ — الترجمة والتوثيق والتلميع

**١. الترجمة** (`lang/ar/resources.php` + `lang/en/resources.php` — مفتاحاً بمفتاح):
```
journal_entries.sections.debit_lines / credit_lines
journal_entries.fields.show_line_details
journal_entries.actions.add_debit_line / add_credit_line
journal_entries.actions.create_payment_order / create_supply_receipt / create_settlement
journal_entries.actions.fill_remaining
journal_entries.helpers.treasury_auto          «تم اختيار حساب الخزينة تلقائياً — تقدر تغيّره.»
journal_entries.placeholders.balanced / remaining
```
+ إضافة المفاتيح الجديدة لقائمة `FinanceI18nTest` للتأكد من وجودها بالعربية والإنجليزية.

**٢. التوثيق** — `resources/views/documentation/partials/finance.blade.php` قسم `#journal-entries`:
- إضافة فقرة **«إزاي تفتح الشاشة»**: زر «إضافة قيد يومية» بقى قائمة بأنواع المستند، واختيارك بيحدد النوع تلقائياً.
- استبدال جدول «سطور القيد» بجدولين: **الجانب المدين** و**الجانب الدائن** (الحساب + القيمة)، وسطر يوضح أن مركز التكلفة والملاحظات خلف مفتاح «إظهار…».
- كارت جديد **«اختصارات الإدخال السريع»**: زر (+) لكل جانب للقيود متعددة الأطراف، القيمة الجديدة بتتملى بالفرق المتبقي تلقائياً، زر الآلة الحاسبة 🧮، وزر تكرار السطر.
- كارت **«الخزينة بتتحدد لوحدها»**: أمر صرف ⇒ الخزينة دائن، إيصال توريد ⇒ الخزينة مدين، تسوية ⇒ لا شيء — والحساب يفضل قابل للتغيير.
- نقل شرح **عدّاد التوازن** لأعلى القسم (بقى أعلى الشاشة لا أسفلها).
- تحديث **المثال العملي** ليطابق التدفق الجديد (٤ خطوات بدل ٥).

**٣. التلميع (فاتح/داكن + RTL):**
- عدّاد التوازن بألوان Filament الدلالية بدل `#059669`/`#e11d48`.
- التأكد أن شبكة العمودين تنهار لعمود واحد على الموبايل، وأن ترتيب مدين/دائن صحيح في RTL.
- فحص القسم في الوضعين الفاتح والداكن (لا ألوان رمادية باردة — التزاماً بـ `dark-theme-tokens`).

**٤. وصفة الكاش بعد التنفيذ:**
```
php artisan optimize:clear
php artisan filament:clear-cached-components
php artisan icons:clear
php artisan permission:cache-reset
npm run build
php artisan queue:restart
```

---

## 6. أثر التعديل على المكاسب (لماذا سيصبح الإدخال أسرع فعلاً)

| العملية | قبل | بعد |
|---|---|---|
| فتح شاشة إضافة قيد صرف | نقرة + اختيار النوع من قائمة | **نقرتان مباشرة** والنوع محدد |
| اختيار حساب الخزينة | بحث واختيار يدوي كل مرة | **تلقائي** |
| حقول ظاهرة لكل سطر | ٥ حقول على ٣ صفوف | **حقلان على صف واحد** |
| كتابة قيمة الطرف المقابل | كتابة الرقم يدوياً + التأكد من التوازن | **مُعبَّأة تلقائياً بالفرق** |
| اختيار مدين/دائن لكل سطر | قائمة منسدلة في كل سطر | **ملغاة** — الجانب يحدده مكان السطر |
| قيد بسيط (مدين + دائن) | ~١٢ تفاعل | **~٤ تفاعلات** |

---

## 7. ما لن يتغير (ضمانات)

- ✅ مخطط قاعدة البيانات — لا Migration.
- ✅ دفتر الأستاذ، اليومية التحليلية، كشف الحساب، ميزان المراجعة، مركز تكلفة العملية، إقفال COGS — كلها تقرأ `direction` كما هي.
- ✅ القيود المولَّدة تلقائياً (الدفعات، أذون الإهلاك، الإقفال) — لا تمر بالفورم أصلاً.
- ✅ قواعد الترحيل والتوازن والصلاحيات — كما هي.
- ✅ اختبارات `JournalEntryTest` الحالية — لا تلمس الفورم ⇒ تظل خضراء.

---

## 8. المخاطر ومعالجتها

| الخطر | المعالجة |
|---|---|
| ضياع `project_id`/`line_notes` عند إخفائهما | `->dehydratedWhenHidden()` + إظهار تلقائي لو الحقل يحمل قيمة + اختبار مخصّص. |
| سطور يتيمة بعد التعديل | `syncLines` داخل `DB::transaction` مع حذف صريح لما لم يعد موجوداً + اختبار «حذف + تعديل + إضافة» في نفس الحفظ. |
| نافذة «عرض» تظهر سطوراً فارغة | `mutateRecordDataUsing` على `ViewAction`. |
| شجرة حسابات بدون كود `1010` | `treasuryAccountFor` ترجع `null` بصمت — الفورم يعمل عادي. |
| تعبئة الفرق تكتب فوق رقم كتبه المستخدم | التعبئة تحدث **فقط عند إنشاء سطر جديد** (default) أو بضغط زر الآلة الحاسبة صراحةً. |
