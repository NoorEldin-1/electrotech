# خطة: تعديلات ورقة الجودة (بيانات العملية + بنود الاختبار)

> **المصدر:** `مكتب ادارة المشروعات.pptx` (11 سلايد) — جولة تعديلات جديدة على قسم التصنيع/مكتب ادارة المشروعات.
> **هذا الملف يغطّي جزءاً واحداً فقط بتفصيل التنفيذ الكامل:** السلايدات **2 و3 و4 — «ورقة الجودة»**:
> - سلايد 2: ملء بيانات العملية تلقائياً بناءً على أمر التصنيع بعد اختياره.
> - سلايد 3: إضافة حقول ناقصة (مساحة المقطع E / الجسم الخارجى / درجة الحماية / الدهان / الطراز / الأمبير) + **حذف «نوع التوصيل»**.
> - سلايد 4: بنود الاختبار — تحويل (الجودة الظاهرية / التجميع / تربيط الجانب الأرضى) إلى **checkboxes**، إبقاء «المقاس المطلوب» حقلاً عادياً، وجعل بقية خانات الاختبار **قراءتين لكل خانة**، وإضافة خانة «تربيط الجانب الأرضى PE–FE».
>
> باقي أجزاء الجولة (السلايدات 1، 5، 6، 7–9، 10–11) خارج نطاق هذا الملف — سلايد 6 (Standard BOM) و7–8 (الصرف) **مُنفَّذة بالفعل** (انظر `PMO_StandardBOM_Plan.md`)؛ الباقي مذكور كخريطة طريق في النهاية.
> يتبع نفس عُرف الخطط: تحليل → قرارات تصميم → ملفات → RBAC → i18n → ثيم/PDF → اختبارات → كاش → معايير قبول.

---

## 1) نصّ المتطلب (السلايدات 2–4)

**سلايد 2 (بيانات العملية):**
> «عند إضافة ورقة الجودة يتم كتابة أمر التشغيل (أمر التصنيع)، ومَلْء بقية الخانات تلقائياً بناءً على أمر التصنيع — مثل: اسم العملية، نوع الموصل، مساحة المقطع، عدد الأقطاب … وهكذا.»

**سلايد 3 (استكمال بيانات العملية):**
> «فى ورقة الجودة متبقّى بعض الخانات التى لم تُضَف، مثل: مساحة المقطع E، الجسم الخارجى، درجة الحماية، الدهان، الطراز، والأمبير. **وحذف «نوع التوصيل».**»

**سلايد 4 (بنود الاختبار):**
> «هناك بعض الخانات غير موجودة، مثل: تربيط الجانب الأرضى PE–FE.
> (الجودة الظاهرية / التجميع / تربيط الجانب الأرضى) كلها تكون بعلامة صح **CHECK BOX**.
> (المقاس المطلوب) خانة عادية كما هى.
> بقية الخانات فيها **قراءتان**، فيجب إدخال قراءتين فى كل خانة (مثل ورقة الجودة المُرسَلة سابقاً).»

**خلاصة المتطلب في ثلاث جُمَل:**
1. بيانات العملية (الرأس) تُملأ **تلقائياً من أمر التصنيع** بمجرد اختياره.
2. الرأس يحتاج **6 حقول جديدة** (E، الجسم الخارجى، درجة الحماية، الدهان، الطراز، الأمبير) و**حذف نوع التوصيل**.
3. شبكة البنود: 3 خانات **صح/خطأ**، خانة نصية واحدة (المقاس)، وكل خانة اختبار كهربى **قراءتان**، مع خانة أرضى PE–FE جديدة.

---

## 2) الوضع الحالي في الكود (ما الموجود فعلاً)

| العنصر | الحالة | المرجع |
|---|---|---|
| نموذج ورقة الجودة | ✅ موجود | `app/Models/QualitySheet.php` |
| بنود الورقة | ✅ موجود (كلها `string`) | `app/Models/QualitySheetLine.php` |
| مورد Filament | ✅ موجود (فورم + جدول + أكشنات fill/approve/print) | `app/Filament/Resources/QualitySheetResource.php` |
| الخدمة (دورة الحياة) | ✅ موجود | `app/Services/QualitySheetService.php` — `ensureForWorkOrder` / `fill` / `approve` |
| قالب الطباعة PDF | ✅ موجود | `resources/views/pdf/quality-sheet.blade.php` |
| هجرة الجداول | ✅ موجود | `database/migrations/2026_06_30_000001_create_quality_sheets_tables.php` |
| i18n | ✅ موجود | `lang/{en,ar}/resources.php → quality_sheets.*` |
| **مصدر بيانات العملية على أمر التصنيع** | ❌ **غير موجود** | `WorkOrder` لا يحمل أي مواصفات فنية (`fillable`: مشروع/منتج/كميات/تكلفة/تواريخ فقط) |
| **حقول الرأس الجديدة (E/الجسم/الحماية/الدهان/الطراز/الأمبير)** | ❌ غير موجود | — |
| **الملء التلقائي عند اختيار أمر التصنيع** | ❌ غير موجود | `ensureForWorkOrder` يملأ `operation_name` فقط من `project?->name ?? title`؛ الفورم لا يفعل شيئاً عند تغيير `work_order_id` |
| **بنود checkbox / قراءتان لكل خانة / خانة PE–FE أرضى** | ❌ غير موجود | كل البنود `TextInput` مفردة |

**الفجوة الأساسية:** الرأس فيه حقول تُملأ يدوياً اليوم بينما السلايد يريدها **مُشتقّة من أمر التصنيع** — وأمر التصنيع لا يحمل هذه المواصفات أصلاً. لذا الجزء يشمل حتماً **إضافة المواصفات الفنية إلى أمر التصنيع** (مصدر الحقيقة) ثم نسخها للورقة.

---

## 3) قرارات التصميم

### قرار 1 — مصدر بيانات العملية = **أمر التصنيع** (Specs live on the WorkOrder, snapshot-copied to the sheet)

- **✅ المُعتمد:** نضيف المواصفات الفنية إلى `work_orders` (يؤلّفها مكتب ادارة المشروعات وقت كتابة أمر التصنيع — سلايد 6)، ثم تُنسَخ **لقطةً واحدةً** إلى ورقة الجودة عند إنشائها/اختيار أمرها. الورقة تعرضها مملوءة مسبقاً وقابلة للتعديل من الجودة عند اللزوم.
  **لماذا:** يطابق حرفياً سلايد 2 «تُملأ تلقائياً بناءً على أمر التصنيع». ومكتب المشروعات هو من يكتب الأمر (سلايد 6/10) فهو المكان الطبيعي للمواصفات. النسخ اللقطي متسق مع نمط «Copy-on-select» المعتمد في `PMO_StandardBOM_Plan.md` (قرار 3) — يحفظ ما كان صحيحاً وقت الاختبار دون ربط حيّ هشّ.

- **🔁 البديل المرفوض (أ) — المواصفات على المنتج (`Item`) لا على الأمر:** أنظف نظرياً (مواصفة المنتج ثابتة)، لكن السلايد يقول «من أمر التصنيع» صراحةً، وبعض القيم (الأمبير/عدد الأقطاب) قد تختلف بين أمر وآخر لنفس المنتج. مرفوض الآن (يمكن لاحقاً جعل الأمر يرث افتراضياته من المنتج).
- **🔁 البديل المرفوض (ب) — إبقاء المواصفات على الورقة فقط (إدخال يدوي بحت):** أبسط (بلا هجرة على `work_orders`)، لكنه يخالف «تُملأ تلقائياً» — يبقى كل شيء يدوياً. مرفوض.

### قرار 2 — النسخ اللقطي عبر الخدمة + ملء الفورم عند تغيير الأمر

- **✅ المُعتمد:** مصدرا الملء:
  1. **عند الإنشاء التلقائي** (`ensureForWorkOrder` — عند «انتهاء التصنيع») تُنسخ المواصفات من الأمر إلى الورقة.
  2. **عند الإنشاء اليدوي من الفورم** (اختيار `work_order_id`) → `live()` + `afterStateUpdated` يستدعي مساعِداً يجلب مواصفات الأمر ويملأ حقول الرأس بـ`$set(...)` — فقط إن كانت فارغة (لا يدهس تعديلاً موجوداً).
  **لماذا:** يغطي المسارين (تلقائي عند انتهاء التصنيع، ويدوي عند إنشاء ورقة من المورد) دون تكرار منطق.

### قرار 3 — حذف «نوع التوصيل» = إزالة من الواجهة + إسقاط العمود

- **✅ المُعتمد:** حذف `connection_type` من الفورم وPDF وi18n، وإسقاط العمود من `quality_sheets` في هجرة (البيانات تجريبية في هذه المرحلة). لو خشينا فقد بيانات إنتاجية لاحقاً نكتفي بالإخفاء — لكن السلايد يقول «حذف» فنسقطه.

### قرار 4 — بنود الاختبار: أعمدة boolean + قراءتان لكل اختبار

- **✅ المُعتمد على `quality_sheet_lines`:**
  - `visual_quality`, `assembly` → **`boolean`** (Checkbox/Toggle).
  - `earth_bond_pe_fe` → **`boolean` جديد** («تربيط الجانب الأرضى PE–FE»).
  - `required_size` → يبقى **`string`** كما هو.
  - كل خانة اختبار كهربى تُقسَّم إلى قراءتين: `test_pe_l123n` → `test_pe_l123n_r1` + `test_pe_l123n_r2`، وبالمثل لـ`test_fe_l123n` / `test_n_l12l3` / `test_l1_l2l3` / `test_l2_l3`. (5 اختبارات × قراءتان = 10 أعمدة نصية).
  **لماذا:** يطابق سلايد 4 بند-ببند. تقسيم الأعمدة (لا JSON) يبقي الطباعة والاستعلام بسيطين ومتسقين مع نمط الأعمدة الحالي.

- **🔁 البديل المرفوض — عمود JSON `readings`:** أقل هجرات لكنه يعقّد الطباعة والفلترة ويكسر تناسق «عمود لكل قيمة» القائم. مرفوض.

---

## 4) التغييرات بالملفات

### (أ) قاعدة البيانات — 3 هجرات جديدة

**هجرة 1** `..._add_specs_to_work_orders.php` (مصدر الحقيقة للمواصفات):
- إضافة أعمدة nullable إلى `work_orders`: `conductor_type` (string), `cross_section` (string), `cross_section_e` (string), `external_body` (string), `protection_degree` (string), `paint` (string), `model` (string), `ampere` (string), `poles_count` (unsignedInteger).
- (`operation_name` يبقى مشتقّاً من `title`/`project.name` — لا عمود جديد له.)

**هجرة 2** `..._add_specs_to_quality_sheets.php`:
- إضافة الأعمدة الجديدة (لقطة): `cross_section_e`, `external_body`, `protection_degree`, `paint`, `model`, `ampere` (كلها string nullable).
- **إسقاط** `connection_type`.

**هجرة 3** `..._restructure_quality_sheet_lines.php`:
- تغيير `visual_quality` و`assembly` إلى `boolean` (افتراضي `false`). *(الأنظف: إسقاط ثم إضافة boolean لتفادي تعقيد `->change()` بين النوعين — البيانات تجريبية.)*
- إضافة `earth_bond_pe_fe` boolean default false.
- إسقاط `test_pe_l123n` / `test_fe_l123n` / `test_n_l12l3` / `test_l1_l2l3` / `test_l2_l3`، وإضافة نظيرَي `_r1`/`_r2` لكلٍّ منها (string nullable).
- إبقاء `required_size`, `piece_number`, `label`, `notes`, `line_no` كما هى.

### (ب) Models

- `app/Models/WorkOrder.php`: `fillable` += المواصفات التسعة؛ `casts` += `poles_count => integer`. *(لا حاجة لإضافتها إلى `syncWritableFields` — المكتب يؤلّفها لا الأرضية.)*
- `app/Models/QualitySheet.php`: `fillable` += (`cross_section_e`, `external_body`, `protection_degree`, `paint`, `model`, `ampere`) و**حذف** `connection_type`.
- `app/Models/QualitySheetLine.php`:
  - `fillable`: حذف الخمسة القديمة + `visual_quality`/`assembly` تبقى بالاسم لكن كـbool؛ إضافة `earth_bond_pe_fe` + العشرة `_r1/_r2`.
  - `casts` += `visual_quality => bool`, `assembly => bool`, `earth_bond_pe_fe => bool`.

### (ج) Service — `app/Services/QualitySheetService.php`

- استخراج مساعِد `specSnapshotFrom(WorkOrder $wo): array` يُرجع مصفوفة قيم الرأس (المواصفات التسعة + `operation_name` المشتق).
- `ensureForWorkOrder()` يستخدمه عند `QualitySheet::create([...])` لملء اللقطة (بدل ملء `operation_name` وحده).
- بذر البنود الافتراضية يبقى كما هو (10 صفوف)، مع الأعمدة الجديدة فارغة/false.

### (د) Filament — `app/Filament/Resources/QualitySheetResource.php`

- **قسم «بيانات العملية»:**
  - `work_order_id` → `live()` + `afterStateUpdated(fn ($state, Set $set) => …)` يجلب مواصفات الأمر ويملأ حقول الرأس إن كانت فارغة.
  - إضافة حقول: `cross_section_e`, `external_body`, `protection_degree`, `paint`, `model`, `ampere` (كلها `TextInput`).
  - **حذف** حقل `connection_type`.
  - (اختياري) جعل الحقول المُشتقة `->placeholder(__('resources.common.auto_from_work_order'))` للإشارة لمصدرها.
- **قسم «بنود الاختبار» (Repeater):**
  - `visual_quality`, `assembly`, `earth_bond_pe_fe` → `Toggle`/`Checkbox`.
  - `required_size` يبقى `TextInput`.
  - كل اختبار كهربى → `Fieldset` بعنوان الاختبار يحوى `TextInput ..._r1` + `TextInput ..._r2` (بعناوين «قراءة 1 / قراءة 2»)، لتجميع القراءتين بصرياً. ضبط `->columns()` بما يناسب العرض.
  - إبقاء `disabled(fn ($record) => $record?->isApproved())` كما هو.

### (هـ) الطباعة — `resources/views/pdf/quality-sheet.blade.php`

- كتلة `meta` (الرأس): حذف سطر «نوع التوصيل»، وإضافة صفوف للحقول الستة الجديدة.
- شبكة `grid`: تحويل أعمدة الصح إلى `✔ / �’’’` (أو `☑/☐`)، ودمج كل اختبار في خليّة تعرض «r1 / r2»، وإضافة عمود PE–FE الأرضى. مراجعة عرض الأعمدة (`th width`) لأن العدد زاد.

### (و) Policy / RBAC

- **لا سياسة/صلاحية جديدة.** الورقة تُدار بـ`QualitySheetPolicy` القائمة، وأمر التصنيع بصلاحيات `work_orders.*` القائمة (المكتب يملكها). المواصفات على الأمر تحرَّر ضمن نفس فورم أمر التصنيع.

---

## 5) i18n (EN/AR بتطابق تام)

- `lang/{en,ar}/resources.php → quality_sheets`:
  - `fields`: إضافة `cross_section_e`, `external_body`, `protection_degree`, `paint`, `model`, `ampere`, `earth_bond_pe_fe`, ومفاتيح القراءتين `test_*_r1`/`test_*_r2` (أو مفتاح اختبار + لاحقتا reading). **حذف** `connection_type`.
  - `pdf`: نفس الإضافات + حذف `connection_type` + عناوين القراءتين.
  - (اختياري) `common.auto_from_work_order`.
- `lang/{en,ar}/work_orders` (أو `resources.work_orders.fields`): تسميات المواصفات التسعة على فورم أمر التصنيع (بعضها موجود بالفعل مثل `conductor_type`/`cross_section` — يُعاد استخدامه).
- تأكيد تكافؤ عدد المفاتيح EN == AR بعد الإضافة/الحذف.

## 6) الثيم / RTL / PDF

- لا ألوان hex في الفورم؛ مكوّنات Filament الدلالية فقط + `__()` في كل النصوص (RTL + light/dark تلقائي).
- PDF: القالب أصلاً `dir` حسب اللغة؛ نحافظ على ألوانه الحالية (`#D9723B`/الرمادي) ونضيف الأعمدة فقط.

---

## 7) الاختبارات — `tests/Feature/Manufacturing/QualitySheetSpecsTest.php` (جديد) + توسيع القائم

- `ensureForWorkOrder` ينسخ المواصفات التسعة من أمر التصنيع إلى الورقة (لقطة).
- تعديل مواصفة على الورقة **لا** يغيّر مواصفة أمر التصنيع (ثبات اللقطة) والعكس.
- بند الاختبار يحفظ `visual_quality`/`assembly`/`earth_bond_pe_fe` كـbool، والقراءتين `_r1`/`_r2` لكل اختبار.
- طباعة PDF لا تشير إلى `connection_type` (تأكيد الحذف) وتعرض الأعمدة الجديدة.
- إبقاء اختبارات دورة الحياة القائمة (fill/approve/finish) خضراء (لم يتغيّر سلوكها).
- تحديث `QualitySheetFactory` / `QualitySheetLineFactory` (إن وُجدا) للأعمدة الجديدة.

## 8) الكاش (بعد التنفيذ)

`php artisan optimize:clear; filament:clear-cached-components; icons:clear; migrate; permission:cache-reset; npm run build; queue:restart`
(`migrate` للهجرات الثلاث، `npm run build` احتياطاً لتغيّرات الفورم.)

---

## 9) معايير القبول (Definition of Done)

- [ ] عند اختيار أمر التصنيع في ورقة الجودة، تُملأ بيانات العملية (اسم العملية/نوع الموصل/مساحة المقطع/عدد الأقطاب/…) تلقائياً منه.
- [ ] حقول الرأس الجديدة موجودة (مساحة المقطع E / الجسم الخارجى / درجة الحماية / الدهان / الطراز / الأمبير)، و«نوع التوصيل» **محذوف** من الفورم والطباعة.
- [ ] في بنود الاختبار: الجودة الظاهرية / التجميع / تربيط الجانب الأرضى = **checkboxes**؛ المقاس المطلوب حقل عادى؛ كل خانة اختبار كهربى تقبل **قراءتين**؛ وخانة PE–FE الأرضى موجودة.
- [ ] الطباعة (AR/EN) تعكس كل ما سبق بشكل سليم.
- [ ] i18n (EN==AR) + light/dark/RTL + الاختبارات خضراء + وصفة الكاش اتنفّذت.

---

## ملحق — خريطة باقي أجزاء الجولة (خارج نطاق هذا الملف)

| الجزء | السلايد | الحالة/ملاحظة |
|---|---|---|
| إعادة التسمية | 1 | «أوامر التشغيل»→«أوامر التصنيع»، «المكتب الفنى»→«مكتب ادارة المشروعات» — i18n بحت. لم يُنفَّذ. |
| Standard BOM + المرونة اليدوية | 6 | **مُنفَّذ** (uncommitted) — `PMO_StandardBOM_Plan.md`. |
| صرف من أمر التصنيع + تكلفة تلقائية | 7–8 | **مُنفَّذ** ضمن نفس تغيير BOM (rewire `IssueVoucherService`). |
| تقرير الإنتاج والفاقد | 9 | يبني على «المخطط=تكلفة خامات الأمر». لم يُنفَّذ. |
| ملخص الأدوار + إضافة التام للمخزون | 10–11 | توثيق أدوار + مخزون المنتج التام عند انتهاء التصنيع. لم يُنفَّذ. |

> **ملاحظة تبعية:** هذا الجزء (ورقة الجودة) مستقلّ — لا يعتمد على BOM ولا يعيق أجزاء 9/10–11.
