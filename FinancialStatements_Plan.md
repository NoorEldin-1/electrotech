# خطة تنفيذ: القوائم المالية — «ما بعد ميزان المراجعة» (ماليات.pptx)

> جولة `ماليات.pptx` — 9 سلايدات. العنوان الحرفي للسلايد الأول:
> **«ما بعد ميزان المراجعة — مرتبط بالاقفال السنوى»**.
> النظام يقف اليوم عند **ميزان المراجعة**. هذه الجولة تبني ما بعده.

---

## 1. تحليل الصور سلايد بسلايد، والربط بالنظام الحالي

| # | السلايد | المطلوب حرفياً | الحالة في النظام قبل هذه الجولة |
|---|---|---|---|
| 1 | العنوان | القوائم المالية التالية لميزان المراجعة، مرتبطة بالإقفال السنوي | ❌ لا يوجد أي قائمة مالية — آخر تقرير هو `TrialBalance` |
| 2 | **قائمة التشغيل** | ت.البضاعة المباعة + م.تشغيل + م.تركيب + م.تصدير + م.وإهلاكات صناعية = **تكلفة المبيعات**. مع ملاحظة: «كله حسابات مسجلة بشجرة الحسابات» | 🟡 الحسابات موجودة (5010/5020/5050/5070) لكن **لا تجميع ولا قائمة** |
| 3 | **قائمة الدخل** | جدول بأعمدة (البيان / جزئى / كلى): صافى المبيعات = المبيعات − المردودات؛ − تكلفة المبيعات = **مجمل الربح**؛ + إيرادات متنوعة + فروق عملة = **إجمالي الإيرادات**؛ − م.عمومية − فروق عملة − م.وإهلاكات − اعتمادات سبق إقفالها − فوائد مدينة = **إجمالي المصروفات**؛ = **صافى الربح** | ❌ غير موجود. **ولا يوجد حساب مردودات مبيعات أصلاً** |
| 4 | **قائمة المركز المالى** | الأصول المتداولة − الالتزامات المتداولة = **رأس المال العامل**؛ + الأصول الثابتة = **إجمالي الاستثمار**. وسهمان: «حساب يخرج من المعادلات» | ❌ غير موجود |
| 5 | قاعدة العرض | «مكتوب اسم الحساب **الرئيسى** ويتم وضع اى حساب **فرعى داخل** الحساب الرئيسى» | ✅ `accounts.parent_id` موجود — لكن **لا أحد يستخدمه في تقرير** |
| 6 | صورتان لقائمة مركز مالي حقيقية للشركة (31 ديسمبر 2026) | البنية الفعلية المطلوبة: أصول ثابتة بأعمدة (التكلفة / مجمع الإهلاك / الصافى)، ثم أصول متداولة، ثم التزامات متداولة، ثم «يتم تمويلة على النحو التالى» (رأس المال + الاحتياطيات والمخصصات + أرباح الفترة)، وحاشية «يوجد شيكات ضمانه بمبلغ 000» | ❌ غير موجود. **لا يوجد مفهوم مجمع إهلاك ولا رأس مال ولا احتياطيات في شجرة الحسابات** |
| 7 | **تصنيف العملاء والموردين** | «يجب تقسيمهم الى نوعين: (عملاء مدينة) و (عملاء دائنة – دفعات مقدمة). المدينة اذا كان الرصيد موجب ودائنة اذا كان سالب… وده هيفرق فى قائمة المركز المالى». نفس الكلام للموردين. **+ «يجب عمل مراكز تكلفة + خط زمنى لكل مشروع بالعميل»** | 🟡 مراكز التكلفة ✅ منفّذة (العملية = مركز التكلفة). التقسيم مدين/دائن ❌. الخط الزمني ❌ |
| 8 | **قائمة التدفقات النقدية** | «الهدف تعديل حساب صافى الربح» — تبدأ بصافى الربح المأخوذ من قائمة الدخل ثم إضافة/طرح بقية الحسابات | ❌ غير موجود |
| 9 | صورة قائمة تدفقات حقيقية + قواعد الحساب | صافى الربح ← من قائمة الدخل؛ − الإهلاك والمخصصات؛ + الأرباح الرأسمالية = **أرباح التشغيل قبل التغير في رأس المال العامل**. ثم التغيرات بقاعدة الاحتمالات الأربعة. ثم أنشطة الاستثمار (أصول ثابتة)، ثم التمويل (جاري شركاء)، ثم **صافى التغير في النقدية + رصيد أول الفترة = رصيد آخر الفترة، ويتم مطابقته مع الواقع** | ❌ غير موجود |

### الخلاصة التحليلية

المطلوب ليس أربعة تقارير منفصلة، بل **طبقة واحدة ناقصة**: النظام يعرف *رصيد* كل حساب، لكنه **لا يعرف مكان الحساب في القائمة المالية**.

`AccountType` الحالي (أصل/التزام/حقوق ملكية/إيراد/مصروف) يحدّد **طبيعة** الحساب (مدين/دائن) فقط، وهو **أخشن من أن يبني قائمة**:

- كل الأصول عنده نوع واحد — فلا يفرّق بين **أصل ثابت** و**أصل متداول** و**نقدية** (والتدفقات النقدية تحتاج التفرقة الثلاثية هذه بالضبط).
- كل المصروفات عنده نوع واحد — فلا يفرّق بين مصروف يدخل **تكلفة المبيعات** (قائمة التشغيل) ومصروف **عمومي** ومصروف **تمويلي**، مع أن سلايد 3 يضعهم في ثلاثة أسطر مختلفة.

⇒ **حجر الأساس: بُعد تصنيف جديد على الحساب اسمه «بند القوائم المالية» (`statement_section`).** كل شيء آخر في هذه الخطة مبني عليه.

---

## 2. القرارات التصميمية (مع التبرير)

### ق1 — بُعد تصنيف جديد `statement_section` بدل توسيع `AccountType`
`AccountType` يقود الطبيعة المدينة/الدائنة وترحيل القيود وميزان المراجعة. توسيعه يكسر ما يعمل. البُعد الجديد **عرضي بحت** ولا يمسّ الترحيل، فالميزان والأستاذ يبقيان كما هما حرفياً.

**قيمة افتراضية آمنة**: أي حساب بلا تصنيف يأخذ التصنيف المشتق من نوعه (أصل ⇒ أصول متداولة، التزام ⇒ التزامات متداولة، …) فلا يختفي حساب من القائمة أبداً لمجرد أن المحاسب لم يصنّفه بعد.

### ق2 — أسطر القائمة = الحسابات الرئيسية، والفرعية تتجمّع داخلها (تنفيذ حرفي لسلايد 5)
سطر القائمة ليس ثابتاً في الكود، بل هو **حساب رئيسي في شجرة الحسابات** ضمن القسم، ورصيده = رصيده + أرصدة كل أبنائه. هذا يعني أن المحاسب يضيف بنداً جديداً للقائمة **بإضافة حساب**، بدون برمجة.

### ق3 — مجمع الإهلاك يرتبط بأصله عبر `contra_of_account_id`
سلايد 6 يعرض الأصول الثابتة بثلاثة أعمدة (التكلفة / مجمع الإهلاك / الصافى). حتى يعرف النظام أي مجمّع يخصم من أي أصل، يُضاف عمود ربط صريح. مجمع غير مربوط يظهر في سطر «غير موزّع» بدل أن يُبتلع صامتاً.

### ق4 — التقسيم مدين/دائن للعملاء والموردين يتم **من دفتر الأطراف** لا من حساب المراقبة
حساب المراقبة (1200 العملاء) له رصيد واحد مجمّع، ولا يمكن تقسيمه من داخله. النظام يملك دفتر أطراف مستقلاً (`account_entries`) برصيد لكل عميل/مورد على حدة، فمنه يُحسب:
- Σ الأرصدة الموجبة ⇒ سطر **عملاء (مدينة)** في الأصول المتداولة
- Σ |الأرصدة السالبة| ⇒ سطر **عملاء – دفعات مقدمة** في الالتزامات المتداولة

وبما أن دفتر الأطراف قد لا يساوي حساب المراقبة (فرق توقيت أو قيد يدوي)، **يُعرض شريط مطابقة صريح** بالفرق بدل إخفائه. الإخفاء هنا كذب محاسبي.

### ق5 — تعارض داخل سلايد 9 نفسه، والحسم لصالح **شرط المطابقة**
السلايد يطلب شيئين لا يجتمعان:

1. **الصيغة المكتوبة** (مرتين: في السهم «يتم **خصم** الاهلاك والمخصصات»، وفي الفقاعة «صافى الربح **–** الاهلاك والمخصصات **+** ارباح راسمالية»).
2. **شرط الاختبار في آخر السلايد**: «ويساوى رصيد النقدية اخر الفترة **ويتم مطابقته مع الواقع**».

بما أن حسابات النتيجة (إيرادات/مصروفات) ليس لها مقابل في الميزانية قبل الإقفال السنوي، فإن الرصيد المستخرج يساوي الرصيد الفعلي **إذا وفقط إذا**:
- **أُضيف** الإهلاك والمخصصات (نقّصا الربح دون أن يخرجا نقدية)، و
- **لم تُعدَّل** الأرباح الرأسمالية إطلاقاً (متحصلاتها دخلت النقدية كقبض عادي، ولا يوجد بند بيع أصل مستقل في أنشطة الاستثمار تُنقل إليه).

بالصيغة الحرفية ينحرف الرصيد بمقدار **٢×(الإهلاك + المخصصات) + الأرباح الرأسمالية** — وقد تحقّق ذلك عملياً على البيانات التجريبية: انحراف **720,000** بالضبط = 2×310,000 + 2×90,000 − 80,000.

**⇒ الافتراضي يتبع شرط المطابقة**، لأنه الاختبار الذي وضعه العميل بنفسه في أسفل نفس الورقة. والصيغة الحرفية تبقى متاحة بمفتاح واحد (`FINANCE_CASH_FLOW_ADD_BACK=false`) للمقارنة، **والشاشة تكتب أسفلها بالنص أيّ صيغة تعمل بها** فلا تخمين.

### ق6 — الاحتمالات الأربعة تُختزل إلى قاعدة واحدة
سلايد 9 يذكر أربع حالات. رياضياً هي قاعدة واحدة:
```
حساب أصول   ⇒ الأثر النقدي = −(رصيد آخر المدة − رصيد أول المدة)
حساب التزام ⇒ الأثر النقدي = +(رصيد آخر المدة − رصيد أول المدة)
```
تُنفَّذ كقاعدة واحدة، ويُعرض في الشاشة عمود «الاتجاه» ليرى المستخدم أي احتمال من الأربعة انطبق.

### ق7 — «حساب يخرج من المعادلات» (سلايد 4) و«يوجد شيكات ضمانه» (سلايد 6) → تصنيفان صريحان
- `excluded` — يخرج تماماً (مثال: ح/صافي الربح 3010، لأن ربح الفترة **يُحسب** من قائمة الدخل، وإدراجه كحساب أيضاً يضاعفه).
- `memo` — خارج المعادلات لكنه يُطبع **كحاشية أسفل القائمة**، تنفيذاً حرفياً لحاشية سلايد 6 (شيكات وخطابات الضمان).

### ق8 — أربع شاشات منفصلة لا شاشة واحدة بتبويبات
الديك يعرضها كأربع قوائم لها أسماء متعارف عليها؛ المحاسب يبحث عنها بالاسم في القائمة الجانبية. الكود المشترك يعيش في الخدمات لا في الشاشات، فالتكرار في الشاشات سطحي.

---

## 3. مراحل التنفيذ

### المرحلة 1 — طبقة التصنيف (حجر الأساس)
- `App\Enums\StatementSection` — 21 حالة تغطي القوائم الأربع + `excluded` + `memo`، مع:
  - `label()` من ملفات الترجمة
  - `defaultForType(AccountType)` — الاشتقاق الآمن
  - `cashFlowRole()` — تشغيلي / استثماري / تمويلي / غير نقدي / نقدية
- هجرة: `statement_section` (nullable, index) + `contra_of_account_id` (nullable FK) على `accounts`.
- `Account`: cast، `effectiveStatementSection()`، علاقتا `contraOf`/`contraAccounts`، و`descendantIds()`.
- `AccountResource`: حقل اختيار + عمود + فلتر + حقل «مجمع إهلاك لحساب».
- `ChartOfAccountsSeeder`: تصنيف كل الحسابات القائمة (**بدون دهس اختيار المحاسب** — يملأ الفارغ فقط) + إضافة الحسابات التي تفرضها الصور ولا وجود لها: مردودات المبيعات، الأصول الثابتة بالتفصيل (أراضي/مباني/معدات/أجهزة)، مجمعات الإهلاك، رأس المال، الاحتياطيات والمخصصات، جاري شركاء، أوراق قبض، أرباح رأسمالية، مصروفات وإهلاكات صناعية، فوائد مدينة، اعتمادات سبق إقفالها (كمصروف).

### المرحلة 2 — محرك الأرصدة + خدمات القوائم الأربع
- `AccountBalanceService` — استعلام مجمّع واحد لكل التواريخ المطلوبة (بدل استعلام لكل حساب كما في `GeneralLedgerService`)، يوفّر: حركة الفترة، رصيد آخر المدة، رصيد أول المدة، والتجميع الهرمي.
- `OperatingStatementService` → قائمة التشغيل (سلايد 2)
- `IncomeStatementService` → قائمة الدخل (سلايد 3) بأعمدة جزئي/كلي
- `BalanceSheetService` → قائمة المركز المالي (سلايدات 4/5/6) + المعادلات + شريط التوازن + الحواشي
- `CashFlowStatementService` → قائمة التدفقات (سلايدات 8/9) + مطابقة النقدية مع الواقع
- `PartyReclassificationService` → تقسيم العملاء/الموردين (سلايد 7)

### المرحلة 3 — سلايد 7 (الشق الثاني): الخط الزمني لكل عملية
- `OperationTimelineService` — يشتق مراحل العملية من البيانات القائمة بلا جداول جديدة: عرض فني ← أمر تشغيل بالمكتب الفني ← تحت التصنيع بالمصنع ← انتهاء التصنيع ← تسليم ← تركيب ← إتمام، بتواريخ كل مرحلة ومدتها.
- يُعرض في ملف تكلفة العملية وفي صفحة العميل.
- عمودان جديدان على شاشتي العملاء والموردين: **طبيعة الرصيد** (مدين/دائن) + القيمة، مع فلتر.

### المرحلة 4 — الشاشات والطباعة
- أربع صفحات Filament تحت «الإدارة المالية» + أربعة عروض Blade.
- `FinancialStatementPdfController` واحد يطبع القوائم الأربع (A4، عربي RTL، شعار الشركة) على نمط `GeneralLedgerPdfController`.

### المرحلة 5 — الصلاحيات والترجمة
- صلاحيات: `operating_statement.view`, `income_statement.view`, `balance_sheet.view`, `cash_flow.view` — تُمنح لـ Admin / Finance / General_Manager.
- ترجمة كاملة عربي + إنجليزي لكل نص جديد (لا نص مكتوب في الكود).

### المرحلة 6 — بيانات تجريبية شاملة
`FinancialStatementsDemoSeeder`: سنة مالية كاملة (2026) بأرصدة أول مدة + قيود مرحّلة تُفعّل **كل سطر** في القوائم الأربع، وعملاء وموردين بأرصدة موجبة **وسالبة** معاً، وأصول ثابتة بمجمع إهلاك، ومسحوبات شركاء، وشيكات ضمان للحاشية.

### المرحلة 7 — الاختبارات
اختبارات وحدة لكل خدمة (بأرقام مُتحقَّق منها يدوياً)، واختبارات شاشة، وRBAC، وi18n.

### المرحلة 8 — تحديث دليل المنصة `/documentation`
قسم جديد داخل «الإدارة المالية» بأربعة موضوعات، ومحدَّث في `DocumentationOutline`.

---

## 4. ما لا تشمله هذه الجولة (وسببه)

| البند | السبب |
|---|---|
| إقفال سنوي فعلي يُصفّر حسابات النتيجة ويرحّل الربح لحقوق الملكية | القوائم أولاً — لا معنى لإقفال قبل أن ترى ما تُقفله. والقوائم تعمل بلا إقفال لأنها تحسب حركة الفترة بالتاريخ. |
| تحويل العملات في القوائم | ميزان المراجعة نفسه لا يحوّل (يجمّع بالعملة)؛ إدخال أسعار الصرف قرار مستقل. القوائم تُبنى بالجنيه، وتنبّه لو وُجدت أرصدة بعملة أخرى. |
| مقارنة سنة بسنة في عمود جانبي | الصورة تعرض عمود 2025 فارغاً (أصفار). القاعدة الزمنية موجودة (الفلاتر بالتاريخ) وإضافته لاحقاً سطر عرض. |

---

## 5. حالة التنفيذ — مكتمل

| المرحلة | الحالة | الدليل |
|---|---|---|
| 1 — طبقة التصنيف | ✅ | `StatementSection` (21 بنداً) + هجرة `statement_section`/`contra_of_account_id`/`party_control` + حقول وفلاتر في شاشة الحسابات + 71 حساباً مصنَّفاً |
| 2 — محرك الأرصدة والخدمات | ✅ | `AccountBalanceService` (استعلام مجمّع واحد لكل تاريخ) + 4 خدمات قوائم + `PartyReclassificationService` |
| 3 — سلايد 7 | ✅ | التقسيم مدين/دائن في القائمة والجداول + `OperationTimelineService` (10 مراحل مشتقّة) |
| 4 — الشاشات والطباعة | ✅ | 4 صفحات + 4 عروض + `FinancialStatementPdfController` (الأربعة تُطبع فعلياً) |
| 5 — الصلاحيات والترجمة | ✅ | 4 صلاحيات مستقلة لـ Admin/Finance/General_Manager + ترجمة كاملة عربي/إنجليزي |
| 6 — بيانات تجريبية | ✅ | `FinancialStatementsDemoSeeder` — سنة 2026 كاملة، وكل سطر في القوائم الأربع يحمل رقماً |
| 7 — الاختبارات | ✅ | 30 اختباراً جديداً، كلها خضراء؛ و194 اختباراً في مجلدَي Finance وGeneralManagement بلا كسر |
| 8 — دليل المنصة | ✅ | 4 أقسام جديدة في `/documentation` + تحديث أقسام الحسابات والعملاء والموردين ومركز التكلفة |

**نتيجتان تُثبتان سلامة البناء على البيانات التجريبية:**
- قائمة المركز المالي: **إجمالي الاستثمار = إجمالي التمويل** بفرق **صفر**.
- قائمة التدفقات النقدية: **الرصيد المستخرج = الرصيد الفعلي** بفرق **صفر**.
