# خطة تنفيذ: إقفال مركز التكلفة في ح/تكلفة البضاعة المباعة

> جولة `Financial Department.pptx` + `قائمة المواد.pptx` (نفس الصور المُرسَلة سابقاً).
> **الجزء المختار: سلايد 12 — مراكز التكلفة** — وهو آخر فجوة متبقّية من الملفين.

---

## 1. تحليل الصور والربط بالنظام الحالي

### أ) صور `قائمة المواد.pptx` (3 سلايدات)

| السلايد | المطلوب | الحالة في النظام |
|---|---|---|
| 1 | شجرة المنتج (BOM) + تفجير الخامات لأمر التشغيل + قياس الانحراف (الهالك) | ✅ منفّذ — `Bom`/`StandardBom`/`WorkOrderMaterialVarianceService` |
| 2 | قيود اليومية بجانبَي مدين ودائن + رقم قيد + رقم مستند + 3 أنواع مستندات ملوّنة | ✅ منفّذ — `JournalEntry` + `DocumentType` + `entry_serial` + دفتر اليومية التحليلي |
| 3 | دفتر أستاذ الخزينة برصيد متحرّك | ✅ منفّذ — `GeneralLedgerService` + كشف الحساب المطبوع |

**⇒ لا فجوة في هذا الملف.**

### ب) صور `Financial Department.pptx` (12 سلايد)

| السلايد | المطلوب | الحالة |
|---|---|---|
| 2–4 | القيود + الترحيل التلقائي للأستاذ + ميزان المراجعة | ✅ منفّذ |
| 5–9 | شجرة الحسابات (أصول/خصوم/مصروفات/إيرادات/حقوق ملكية) | ✅ البنية منفّذة — بيانات لا كود. **استثناء واحد**: ح/**تكلفة البضاعة المباعة** المذكور نصاً في سلايد 8 **غير موجود في `ChartOfAccountsSeeder`** |
| 10 | المبيعات: إذن تسليم ≠ فاتورة + حالة فوترة | ✅ منفّذ (`4b49730`) |
| 11 | المشتريات: إذن إضافة ≠ فاتورة + إقفال بسبب | ✅ منفّذ (`fca68ca`) |
| 12 | **مراكز التكلفة** | 🟡 **الفجوة — هذه الجولة** |

### ج) تفكيك سلايد 12 إلى ثلاث جُمَل، وحالة كل جملة

| # | نص السلايد | الحالة الفعلية في الكود |
|---|---|---|
| 1 | «عند انتقال العملية من قسم المبيعات يتم فتح مركز تكلفة باسم العملية» | ✅ منفّذ — العملية (`Project`) **هي** مركز التكلفة، وصفحة `OperationCostFile` تعرضه، و`JournalEntryLine.project_id` هو بُعد مركز التكلفة في الأستاذ |
| 2 | «ويتم تحميلها بأذون الصرف الخاصة بالمصنع وحساب الفرق بين إذن الصرف وأمر التصنيع (الهالك)» | ✅ منفّذ — `IssueVoucherService::post` يحمّل `project.actual_cost`، و`WorkOrderMaterialVarianceService` يحسب الهالك بالكمية والقيمة |
| 3 | **«وعند تسليم العميل بإذن تسليم يتم إقفال مركز التكلفة في حساب تكلفة البضاعة المباعة»** | ❌ **غير منفّذ إطلاقاً** — `DeliveryVoucherService::activateIfFullyApproved` يخصم المخزون ويقيّد العميل مديناً في `AccountEntry` فقط، ولا يوجد أي قيد يومية يُخرج تكلفة العملية من المخزون إلى المصروفات |

**سبب اختيار هذا الجزء:**
1. **الفجوة الحقيقية الوحيدة الباقية** في الملفين — كل ما عداها منفّذ أو بيانات.
2. **أثره محاسبي جوهري**: بدونه تبقى قيمة الخامات المستهلكة في ح/المخزون (1300) إلى الأبد رغم أن البضاعة خرجت فعلياً للعميل ⇒ **المخزون في ميزان المراجعة مُبالَغ فيه، والمصروفات ناقصة، وصافي الربح كاذب**. هذه ليست ميزة تجميلية بل خطأ في القوائم المالية.
3. **مغلق ومستقل**: يعتمد على بنية موجودة بالكامل (القيد المزدوج + بُعد مركز التكلفة + أذون التسليم)، ولا يحتاج شاشات جديدة.
4. له **سابقة مطابقة في الكود** يُقاس عليها: `DepreciationVoucherService::postLossJournal` (مدين حساب مصروف / دائن المخزون، مع التعطّل الآمن عند غياب الحسابات).

---

## 2. الوضع الحالي (حقائق من الكود)

- `IssueVoucherService::post` — ينقل المخزون خام → تحت التشغيل، ويزيد `project.actual_cost` و`workOrder.actual_material_cost`. **لا قيد يومية.**
- `ReturnVoucherService::post` — العكس تماماً، ويُنقص `actual_cost`. **لا قيد يومية.**
- `DepreciationVoucherService::post` — يخصم من تحت التشغيل **ويُنشئ قيداً** مدين (5060 هالك غير طبيعي / 5010 هالك طبيعي) / دائن (1300 المخزون). الهالك غير الطبيعي يُنقص `actual_cost`، والطبيعي **يظل محمَّلاً على العملية**.
- `DeliveryVoucherService::activateIfFullyApproved` — يخصم من مخزن التام، ويُنشئ `AccountEntry` مديناً للعميل. **لا قيد يومية ولا تكلفة بضاعة مباعة.**
- `OperationCostService` — طبقة تجميع للقراءة فقط (`materialsCost` + `ledgerExpenses` + الإيراد والربح).
- `ChartOfAccountsSeeder` — المصروفات 5010–5060 فقط. **لا يوجد ح/تكلفة البضاعة المباعة.**

**⇒ الخلاصة**: مركز التكلفة يُفتح ويُحمَّل بشكل صحيح، لكنه **لا يُقفل أبداً**.

---

## 3. القرارات التصميمية (مع التبرير)

### ق1 — قيمة الإقفال تُشتق من حركة المخزون لا من `project.actual_cost`
```
الرصيد القابل للإقفال = Σ أذون الصرف المرحّلة
                       − Σ أذون الارتداد المرحّلة
                       − Σ أذون الإهلاك المرحّلة (بنوعيها)
                       − ما سبق إقفاله
```
**السبب الحاسم**: `actual_cost` رقم **إداري** لا محاسبي — الهالك الطبيعي يظل محمَّلاً عليه عمداً (سلايد التصنيع 5) بينما قيمته **غادرت ح/المخزون فعلاً** بقيد الإهلاك. لو أقفلنا بـ`actual_cost` لأصبح ح/المخزون **دائناً مرّتين** بنفس القيمة. الرقمان مختلفان عن قصد، ولكل منهما استخدامه؛ الإقفال يستخدم الرقم المحاسبي.

### ق2 — القيد: مدين تكلفة البضاعة المباعة (5070) / دائن المخزون (1300)
هذا هو **معنى** الجملة الثالثة في السلايد حرفياً. الحساب الجديد `5070` من نوع مصروف، ويُضاف إلى `ChartOfAccountsSeeder` (السلايد 8 يذكره نصاً ضمن المصروفات). القيد من نوع **قيد تسوية** (`Settlement` — أخضر) لأنه لا يحرّك نقدية، وهو تصنيف الملف نفسه لقيود التسوية.

### ق3 — سطر المدين يحمل `project_id` (بُعد مركز التكلفة)، وسطر الدائن لا يحمله
تكلفة البضاعة المباعة **يجب** أن تكون قابلة للتحليل بالعملية — وهذا هو المقصود من «مركز التكلفة» أصلاً. ح/المخزون حساب مراقبة إجمالي فلا يُنسب لعملية.
**⇒ يستلزم تعديلاً واحداً في `OperationCostService::ledgerExpenses`**: استبعاد حساب تكلفة البضاعة المباعة من مصروفات الأستاذ، وإلا حُسبت الخامات مرّتين (مرة في `materialsCost` ومرة في `ledgerExpenses`) وتضاعف إجمالي التكلفة. تكلفة البضاعة المباعة **نتيجة** مركز التكلفة لا مُدخَل فيه.

### ق4 — الإقفال تراكمي (يُقفل الرصيد المتبقّي) لا مرّة واحدة أبدية
جدول `cost_center_closings` يسجّل كل إقفال بقيمته وقيده. الرصيد غير المقفل = المستهلك − المقفل. **السبب**: التسليمات قد تكون جزئية، وقد تصل تكاليف متأخرة بعد أول إقفال؛ نموذج «مرّة واحدة» يترك تلك التكلفة عالقة في المخزون إلى الأبد. النموذج التراكمي يغطي الحالتين بقاعدة واحدة بسيطة.

### ق5 — الإقفال التلقائي عند تفعيل إذن التسليم، بشرطين
السلايد يقول «عند تسليم العميل بإذن تسليم **يتم** إقفال مركز التكلفة» ⇒ تلقائي. لكن الإقفال أثناء استمرار التصنيع يُحمّل تكلفة على المبيعات قبل اكتمالها. لذلك يُنفَّذ الإقفال التلقائي فقط عندما:
1. `config('operations.auto_close_cost_center')` مفعّل (افتراضي **true**)، **و**
2. **لا توجد أوامر تشغيل مفتوحة** للعملية (كلها Completed/Cancelled) ⇒ لن تُحمَّل تكاليف جديدة، **و**
3. الرصيد غير المقفل > 0.
غير ذلك: يبقى المركز مفتوحاً وتُقفله المالية يدوياً. **الإقفال التلقائي لا يُفشل التسليم أبداً** — لو غاب حساب من الشجرة تُتخطّى الخطوة بصمت (نفس فلسفة `DepreciationVoucherService` و`OperationPaymentService`).

### ق6 — الإقفال اليدوي يشترط وجود تسليم فعلي
شرط `close()` اليدوي: للعملية **إذن تسليم واحد على الأقل بحالة Active**. بدون تسليم لا وجود لـ«بضاعة مباعة» — استهلاك خامات في عملية أُلغيت هو **إعدام** لا تكلفة مبيعات، وله مستنده الخاص (إذن الإهلاك). الفارق ليس شكلياً: الأول يضرب حساب المبيعات والثاني حساب الخسائر.

### ق7 — التصحيح بقيد عكسي لا بحذف
القيد المُرحَّل لا يُحذف (`JournalEntry` مُقفل بعد الترحيل). لذا «عكس الإقفال» يُنشئ قيداً معاكساً (مدين المخزون / دائن تكلفة البضاعة المباعة) وسطراً بقيمة سالبة في جدول الإقفالات، فيعود الرصيد غير المقفل تلقائياً. سلسلة التدقيق تبقى كاملة.

### ق8 — صلاحية مستقلة `operations.close_cost_center`
الإقفال **قيد محاسبي في الأستاذ**، أثقل من مجرد قراءة ملف التكلفة (`operations.view_cost`). المالكون: **Finance** + **General_Manager** + Admin. الإقفال التلقائي يعمل بغض النظر عن مُفعِّل التسليم (النظام هو الفاعل، ويُسجَّل `closed_by = null`).

### ق9 — لا شاشة جديدة
كل شيء داخل صفحة **مركز تكلفة العملية** الموجودة: بطاقات جديدة (المستهلك من المخزون / المقفل / غير المقفل) + جدول سجل الإقفالات + إجراءَي الترويسة.

---

## 4. التنفيذ التفصيلي

### 4.1 الإعدادات — `config/operations.php`
```php
'cogs_account_code' => '5070',
'auto_close_cost_center' => env('OPERATIONS_AUTO_CLOSE_COST_CENTER', true),
```

### 4.2 شجرة الحسابات — `ChartOfAccountsSeeder`
سطر جديد: `['5070', 'تكلفة البضاعة المباعة', $expense, 'EGP']` (السلايد 8). `firstOrCreate` ⇒ آمن على البيئات القائمة.

### 4.3 قاعدة البيانات — `2026_07_27_000002_create_cost_center_closings_table`
| العمود | النوع | الغرض |
|---|---|---|
| `project_id` | FK cascade | مركز التكلفة |
| `delivery_voucher_id` | FK nullOnDelete | إذن التسليم المُطلِق (للتلقائي) |
| `journal_entry_id` | FK nullOnDelete | القيد الناتج |
| `reverses_id` | FK self nullOnDelete | يشير للإقفال المعكوس |
| `amount` | decimal(14,2) | موجبة للإقفال، سالبة للعكس |
| `is_automatic` | boolean | مصدر الإقفال |
| `closed_by` | FK users nullOnDelete | null = النظام |
| `closed_at` | timestamp | تاريخ الإقفال |
| `notes` | string nullable | سبب العكس |

### 4.4 الموديل — `app/Models/CostCenterClosing.php`
علاقات `project` / `deliveryVoucher` / `journalEntry` / `closedBy` / `reverses` / `reversal`، ودالتا `isReversal()` و`isReversed()`.
`Project::costCenterClosings()` علاقة `hasMany`.

### 4.5 الخدمة — `app/Services/CostCenterClosingService.php`
| الدالة | الوصف |
|---|---|
| `inventoryConsumed(Project): float` | صرف − ارتداد − إهلاك (ق1) |
| `closedValue(Project): float` | Σ القيم (العكوس سالبة) |
| `unclosedBalance(Project): float` | المستهلك − المقفل |
| `isClosed(Project): bool` | الرصيد ≈ 0 مع وجود إقفال |
| `hasActiveDelivery(Project): bool` | شرط ق6 |
| `hasOpenWorkOrders(Project): bool` | شرط ق5 |
| `close(Project, ?DeliveryVoucher, ?User, bool $automatic): CostCenterClosing` | الإقفال اليدوي — يرمي أخطاءً واضحة |
| `closeOnDelivery(DeliveryVoucher): ?CostCenterClosing` | المسار التلقائي — لا يرمي أبداً |
| `reverse(CostCenterClosing, ?User, ?string $reason): CostCenterClosing` | القيد العكسي (ق7) |
| `postClosingJournal(...)` | private — يبني القيد ويرحّله عبر `JournalEntryService` |

### 4.6 الربط — `DeliveryVoucherService`
سطر واحد داخل معاملة `activateIfFullyApproved` بعد `$voucher->update([... Active ...])`:
`$this->costCenterClosings->closeOnDelivery($voucher);`
(حقن بالمُنشئ). الاستدعاء لا يرمي استثناءً ⇒ التسليم لا ينكسر أبداً بسبب المحاسبة.

### 4.7 القراءة — `OperationCostService`
- استبعاد حساب `cogs_account_code` من `ledgerExpenses()` (ق3) مع تعليق يشرح السبب.
- `breakdown()` يضيف: `inventory_consumed` و`closed_to_cogs` و`unclosed_cost`.

### 4.8 الواجهة — `OperationCostFile` + الـ blade
- ثلاث بطاقات جديدة + **بادج حالة** (مقفل / مقفل جزئياً / مفتوح).
- جدول سجل الإقفالات (التاريخ، القيمة، رقم القيد، إذن التسليم، المستخدم/تلقائي).
- إجراءا ترويسة: **«إقفال مركز التكلفة»** (تأكيد + عرض القيمة) و**«عكس إقفال»** (Select للإقفالات القابلة للعكس + سبب مطلوب).
- كلاهما مشروط بـ`operations.close_cost_center` وبوجود عملية مختارة.
- ألوان سيمانتية + توكنات الوضع الداكن (`var(--surface-2)` … إلخ) طبقاً لاتفاقية `dark-theme-tokens`.

### 4.9 RBAC
`operations.close_cost_center` في `getPermissions()`، وتُمنح لـ **Finance** و**General_Manager** (وAdmin تلقائياً).

### 4.10 i18n
- `lang/{en,ar}/resources.php`: `operations_cost.cards.*` الجديدة + `operations_cost.closing.*` (العنوان، الجدول، الإجراءات، الإشعارات، الحالات).
- `lang/{en,ar}/errors.php`: كتلة `cost_center` (5 مفاتيح).
- `lang/{en,ar}/permissions.php`: وصف الصلاحية.

### 4.11 الاختبارات — `tests/Feature/GeneralManagement/CostCenterClosingTest.php`
1. المستهلك = صرف − ارتداد − إهلاك (بنوعَيه).
2. الإقفال يُنشئ قيداً مُرحَّلاً مدين 5070 / دائن 1300 بالقيمة الصحيحة.
3. سطر المدين يحمل `project_id` والدائن لا يحمله.
4. `ledgerExpenses` **لا** تتضخّم بعد الإقفال (اختبار عدم الازدواج — أهم اختبار في الملف).
5. تفعيل إذن التسليم يُقفل تلقائياً حين لا توجد أوامر تشغيل مفتوحة.
6. ولا يُقفل حين توجد أوامر مفتوحة.
7. ولا يُقفل حين يكون الفلاج مطفأً.
8. غياب الحسابات ⇒ التسليم ينجح بلا قيد (تعطّل آمن).
9. رفض الإقفال اليدوي بلا تسليم فعّال.
10. رفض الإقفال حين الرصيد صفر.
11. تكلفة متأخرة بعد الإقفال ⇒ رصيد غير مقفل جديد يمكن إقفاله بقيد ثانٍ.
12. العكس يُنشئ قيداً معاكساً ويعيد الرصيد، ولا يُعكس إقفال معكوس مرّتين.
13. RBAC + مفاتيح الترجمة en/ar.

### 4.12 المخاطر ومعالجتها
| الخطر | المعالجة |
|---|---|
| ازدواج التكلفة في ملف مركز التكلفة | استبعاد 5070 من `ledgerExpenses` + اختبار صريح (#4) |
| تدويل ح/المخزون مرّتين بسبب الهالك الطبيعي | خصم كل أذون الإهلاك من قاعدة الإقفال (ق1) + اختبار (#1) |
| انكسار التسليم بسبب خطأ محاسبي | المسار التلقائي لا يرمي استثناءً + اختبار (#8) |
| إقفال مبكّر يفسد المطابقة | شرط «لا أوامر تشغيل مفتوحة» + الإقفال التراكمي |
| بيئة بلا الحساب 5070 | `firstOrCreate` في الـ seeder + تعطّل آمن + خطأ صريح في المسار اليدوي |
| إقفال مكرّر | القيمة المقفلة تُخصم دائماً من القاعدة ⇒ الإقفال الثاني يجد رصيداً صفرياً ويُرفض |

### 4.13 مسح الكاش محلياً
```powershell
php artisan migrate; php artisan db:seed --class=ChartOfAccountsSeeder; php artisan db:seed --class=RoleAndPermissionSeeder; php artisan optimize:clear; php artisan filament:clear-cached-components; php artisan permission:cache-reset
```
