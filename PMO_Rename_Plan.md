# خطة: إعادة التسمية — «أوامر التشغيل → أوامر التصنيع» و«المكتب الفنى → مكتب ادارة المشروعات» (سلايد 1)

> **المصدر:** `مكتب ادارة المشروعات.pptx` (11 سلايد) — جولة تعديلات على قسم التصنيع/مكتب ادارة المشروعات.
> **هذا الملف يغطّي جزءاً واحداً فقط بتفصيل التنفيذ الكامل:** **سلايد 1 — إعادة التسمية + نقل «أوامر التصنيع» إلى قسم مكتب ادارة المشروعات.**
> باقي أجزاء الجولة **مُنفَّذة بالفعل** (السلايدات 2–4 ورقة الجودة، 5+10 دورة الاعتماد، 6 Standard BOM، 7–9 الصرف/الإنتاج، 11 مخزون التام) — انظر `QualitySheet_Modifications_Plan.md` و`PMO_ApprovalWorkflow_Plan.md` و`PMO_StandardBOM_Plan.md`. **هذا السلايد هو الجزء الوحيد المتبقّى في الجولة.**
> يتبع نفس عُرف الخطط ([[department-workflow]]): تحليل → قرارات تصميم → ملفات → RBAC → i18n → ثيم → اختبارات → كاش → معايير قبول.

---

## 1) نصّ المتطلب (سلايد 1)

> «هيبقى اسمها بدل من **أوامر التشغيل** → **أوامر التصنيع**، وهتكون في قسم **المكتب الفنى**
> (سوف يتم تغيير اسم **المكتب الفنى** إلى **مكتب ادارة المشروعات**).»

**خلاصة المتطلب في ثلاث جُمَل:**
1. مورد «أوامر التشغيل» يُعاد تسميته عرضياً إلى «أوامر التصنيع» (EN: Work Orders → Manufacturing Orders).
2. مجموعة التنقّل «المكتب الفنى» تُعاد تسميتها إلى «مكتب ادارة المشروعات» (EN: Technical Office → Project Management Office).
3. «أوامر التصنيع» تنتقل لتظهر **تحت قسم مكتب ادارة المشروعات** (هى اليوم تحت قسم «التصنيع»).

---

## 2) الوضع الحالي في الكود (ما الموجود فعلاً)

| العنصر | الحالة الحالية | المرجع |
|---|---|---|
| اسم المورد | «أوامر التشغيل» / "Work Orders" | `lang/{ar,en}/resources.php` → `work_orders.{label,plural_label,navigation_label}` (سطر ~1532–1534) |
| قسم المورد في التنقّل | مجموعة **`manufacturing`** («التصنيع») | `WorkOrderResource::getNavigationGroup()` سطر 35 |
| مجموعة `technical_office` تحتوى | **BOM فقط** | `BomResource::getNavigationGroup()` سطر 30 |
| مجموعة `manufacturing` تحتوى | أوامر التشغيل + ورقة الجودة + الإنتاج والفاقد | `WorkOrderResource` / `QualitySheetResource` / `ProductionEntryResource` |
| اسم مجموعة `technical_office` | «المكتب الفني» / "Technical Office" | `lang/{ar,en}/navigation.php` → `groups.technical_office` |
| ترتيب المجموعات في الشريط | ... → `technical_office` (٣) → ... → `manufacturing` (٦) | `AdminPanelProvider::navigationGroups()` سطر 67–76 |
| مفتاح الدور | `Technical_Office` (يُعرَض كما هو أو بلا ترجمة) | `RoleAndPermissionSeeder` سطر 317، `config/operations.php`، اختبارات، `DemoUserSeeder` |
| نص «اعتماد مدير المكتب الفني» | مستخدَم على اعتماد الـ BOM | `resources.php` سطر 577 `boms...approved_by` |
| ودجت لوحة القيادة | "Active Work Orders" / «أوامر التشغيل النشطة» | `lang/ar.json` سطر 6 + الودجت |

**الفجوة الأساسية:** المتطلب **عرضي بحت (تسميات + مكان في القائمة)** — لا يمسّ جدول `work_orders` ولا كلاس `WorkOrder` ولا مفاتيح الصلاحيات `work_orders.*` ولا مفتاح مجموعة التنقّل `technical_office` ولا مفتاح الدور `Technical_Office`. كلها **مُعرّفات داخلية تبقى كما هى**؛ نغيّر **القيم المعروضة** فقط + **سطر واحد** لنقل المجموعة.

---

## 3) قرارات التصميم

> حسب عُرف الخطط: نختار الأقرب للمتطلب بدون سؤال، ونوثّق البديل المرفوض.

### قرار 1 — إعادة التسمية = تغيير قيم i18n فقط، مع إبقاء كل المُعرّفات الداخلية

- **✅ المُعتمد:** تغيير القيم النصّية في ملفات اللغة فقط:
  - `work_orders.label/plural_label/navigation_label` → «أمر تصنيع / أوامر التصنيع» و "Manufacturing Order / Manufacturing Orders".
  - كل ظهور معروض آخر لمصطلح «أمر التشغيل/Work Order» يشير لنفس الكيان (تسميات الصلاحيات view/edit/delete، عناوين الأقسام، إشعارات، عناوين الأعمدة المرجعية `work_order`) → «أمر التصنيع / Manufacturing Order».
  - **تبقى كما هى بلا تغيير:** جدول `work_orders`، كلاس `App\Models\WorkOrder`، أعمدة `work_order_id`، مفاتيح الصلاحيات `work_orders.*`، مفتاح مجموعة التنقّل `technical_office`، مفتاح الدور `Technical_Office`.
  **لماذا:** المطلوب «اسم» معروض فقط. تغيير المُعرّفات الداخلية (جدول/كلاس/صلاحية/مفتاح دور) خطر عالٍ بلا مكسب: يكسر الهجرات والعلاقات والصلاحيات المخزّنة والاختبارات و`config/operations.php`. الفصل بين «المفتاح الثابت» و«التسمية المترجمة» هو نفس النمط المتّبع في كل الموارد.

- **🔁 البديل المرفوض — إعادة تسمية فعلية للجدول/الكلاس/مفتاح المجموعة/الدور:** «أنظف اسمياً» لكنه هجرة ضخمة عالية الخطورة (rename table + foreign keys + إعادة تسمية الدور المخزّن في `model_has_roles` + تحديث `config/operations.php` والاختبارات) لمكسب صفري للمستخدم النهائى. مرفوض.

### قرار 2 — «مكتب ادارة المشروعات» = إعادة تسمية عرض مجموعة `technical_office` (لا مجموعة جديدة)

- **✅ المُعتمد:** تغيير قيمة `navigation.groups.technical_office` إلى «مكتب ادارة المشروعات» / "Project Management Office" مع إبقاء **مفتاح** المجموعة `technical_office`. لا تُنشأ مجموعة جديدة ولا يتغيّر ترتيب المجموعات في `AdminPanelProvider` (المفتاح لم يتغيّر → المصفوفة تظلّ صحيحة).
  **لماذا:** السلايد يقول صراحة «سوف يتم تغيير اسم المكتب الفنى» — أى إعادة تسمية لنفس القسم لا إنشاء قسم منفصل. إبقاء المفتاح يحافظ على كل `getNavigationGroup()` القائمة (BOM يبقى فيها تلقائياً) وعلى ترتيب الشريط.

- **🔁 البديل المرفوض — مجموعة `pmo` جديدة:** يفرض تحديث كل مورد + `navigationGroups()` + i18n لمجموعتين، ويترك «المكتب الفني» يتيماً. مرفوض.

### قرار 3 — نقل «أوامر التصنيع» إلى قسم مكتب ادارة المشروعات = سطر واحد

- **✅ المُعتمد:** تغيير `WorkOrderResource::getNavigationGroup()` من `manufacturing` إلى `technical_office`. **يُنقل مورد أوامر التصنيع فقط** — ورقة الجودة والإنتاج والفاقد **يبقيان** في قسم «التصنيع».
  **لماذا:** السلايد يخصّ «أوامر التصنيع» صراحةً بالنقل. ويطابق توزيع الأدوار (سلايد 10): **مكتب المشروعات** يؤلّف/يعتمد أمر التصنيع والـ BOM (قسم PMO)، بينما **المصنع** يعمل ورقة الجودة ويسجّل الإنتاج (قسم التصنيع). النتيجة: قسم «مكتب ادارة المشروعات» = BOM + أوامر التصنيع (منطقى: هو من يعرّف التركيبة ويكتب الأمر).

- **🔁 البديل المرفوض — نقل ورقة الجودة والإنتاج أيضاً:** يخالف سلايد 10 (الجودة/الإنتاج دور المصنع)، ويُفرغ قسم «التصنيع» بلا مبرر. مرفوض.

### قرار 4 — اسم الدور المعروض `Technical_Office` → «مكتب ادارة المشروعات» (عرض فقط)

- **✅ المُعتمد:** إبقاء **مفتاح** الدور `Technical_Office` (مخزّن ومستخدم في seeders/config/tests) وتغيير **تسميته المعروضة** حيثما تُعرَض للمستخدم إلى «مدير مكتب ادارة المشروعات». يشمل ذلك تحديث نص `boms...approved_by` (سطر 577) من «اعتماد مدير المكتب الفني» إلى «اعتماد مدير مكتب ادارة المشروعات».
  **ملاحظة تحقّق أثناء التنفيذ:** يُفحَص كيف تُعرَض أسماء الأدوار في شاشة الأدوار (Filament Shield). لو كانت تُعرَض من طبقة ترجمة → نحدّثها؛ لو تُعرَض المفتاح الخام `Technical_Office` كما هو → التسمية العرضية للمستخدم تظل عبر النصوص المترجمة (نافيجيشن/الاعتمادات) ولا نلمس المفتاح. (قرار عرضى منخفض الخطر — لا RBAC جديد.)

---

## 4) التغييرات بالملفات

### (أ) i18n — `lang/en/resources.php` و`lang/ar/resources.php`

- كتلة `work_orders` (سطر ~1528+):
  - `label`: "Work Order" → **"Manufacturing Order"** / «أمر التشغيل» → **«أمر تصنيع»**.
  - `plural_label` + `navigation_label`: "Work Orders" → **"Manufacturing Orders"** / «أوامر التشغيل» → **«أوامر التصنيع»**.
- تسميات الصلاحيات (سطر ~2050–2059): `view/edit/delete` «... أوامر التشغيل» → «... أوامر التصنيع» (EN مماثل).
- الظهور المرجعى للكيان في موارد أخرى (`work_order` كعمود/حقل في الأصناف/الصرف/الجودة/الإنتاج، السطور ~778/789/822/833/861/874/1096/1511/1872): «أمر تشغيل/Work Order» → «أمر تصنيع/Manufacturing Order» **حيثما يُعرَض للمستخدم**.
- سطر 577 `boms...approved_by`: «اعتماد مدير المكتب الفني» → «اعتماد مدير مكتب ادارة المشروعات» / EN «... Project Management Office Manager».
- إشعارات/عناوين أخرى تحوى "Work Order" (سطر ~1619/1626/1630 …): تُحدَّث لاتّساق المصطلح.

### (ب) i18n — `lang/en/navigation.php` و`lang/ar/navigation.php`

- `groups.technical_office`: "Technical Office" → **"Project Management Office"** / «المكتب الفني» → **«مكتب ادارة المشروعات»**.
- (المفتاح `technical_office` يبقى.)

### (ج) i18n — `lang/ar.json` (+ نظيره إن وُجد للودجت)

- "Active Work Orders" / «أوامر التشغيل النشطة» → «أوامر التصنيع النشطة» (وأى نص ودجت مماثل).

### (د) التنقّل — `app/Filament/Resources/WorkOrderResource.php`

- `getNavigationGroup()`: `__('navigation.groups.manufacturing')` → `__('navigation.groups.technical_office')` (سطر 35). **سطر واحد.**
- (اختياري) مراجعة `getNavigationSort()` لو وُجد، ليأتى أمر التصنيع بترتيب منطقى داخل قسم PMO (بعد/قبل BOM).

### (هـ) تعليقات فقط — `app/Providers/Filament/AdminPanelProvider.php`

- تحديث التعليق «Technical Office» (سطر 62) ليعكس «Project Management Office» — **تعليق فقط، لا سلوك.** مصفوفة `navigationGroups()` لا تتغيّر (المفتاح ثابت).

### (و) بدون تغيير (مقصود)

- **لا** هجرات، **لا** تغيير على `WorkOrder`/`work_orders`، **لا** تغيير على مفاتيح `work_orders.*`، **لا** تغيير على مفتاح الدور `Technical_Office` أو `config/operations.php` أو الـ seeders أو اختبارات RBAC.

---

## 5) RBAC

- **لا صلاحيات جديدة ولا تغيير أدوار.** المفاتيح كلها ثابتة → لا حاجة لإعادة زرع أو `permission:cache-reset` لأسباب صلاحية (تُذكر في الكاش احتياطاً فقط).
- لا تنطبق ملاحظة «التنصيبات القائمة» ([[department-workflow]]) هنا لعدم وجود صلاحية جديدة.

## 6) i18n (تطابق EN/AR)

- كل التغييرات نصّية ومتناظرة EN↔AR. **تأكيد تكافؤ عدد المفاتيح EN == AR بعد التعديل** (تغيير قيم لا مفاتيح → العدد يجب أن يبقى كما هو). لا مفاتيح جديدة متوقّعة.

## 7) الثيم / RTL

- لا ألوان ولا مكوّنات جديدة؛ نصوص عبر `__()` فقط (RTL + light/dark تلقائى). لا أثر على الثيم.

---

## 8) الاختبارات

- **لا اختبارات منطق جديدة** (تغيير عرضى). التحقّق يدوى بصرى:
  - «أوامر التصنيع» تظهر تحت قسم «مكتب ادارة المشروعات» في الشريط (عربى وإنجليزى) بجوار BOM.
  - قسم «التصنيع» ما زال يحوى ورقة الجودة والإنتاج والفاقد.
  - لا نصّ متبقٍّ يقول «أوامر التشغيل / Work Orders» في القائمة/العناوين/الصلاحيات.
- **تشغيل مجموعة الاختبارات الحالية كاملةً** لتأكيد أن اختبارات RBAC/التصنيع القائمة (التى تستخدم مفتاح الدور `Technical_Office` ومفاتيح `work_orders.*`) **لم تنكسر** (يجب ألّا تنكسر لأن المفاتيح لم تتغيّر).
- فحص نصّى: `grep` على «أوامر التشغيل» و"Work Orders" و«المكتب الفني»/"Technical Office" في `lang/` للتأكد من عدم بقاء ظهور معروض قديم.

## 9) الكاش (بعد التنفيذ)

`php artisan optimize:clear; filament:clear-cached-components; icons:clear; npm run build; queue:restart`
(لا `migrate` — لا هجرة. `optimize:clear` يمسح كاش الترجمة/التهيئة؛ `filament:clear-cached-components` لالتقاط تغيّر مجموعة التنقّل.)

---

## 10) معايير القبول (Definition of Done)

- [ ] المورد يظهر باسم **«أوامر التصنيع» / "Manufacturing Orders"** في الشريط والعناوين وتسميات الصلاحيات (عربى وإنجليزى).
- [ ] مجموعة التنقّل تظهر باسم **«مكتب ادارة المشروعات» / "Project Management Office"**.
- [ ] **«أوامر التصنيع» تقع داخل قسم «مكتب ادارة المشروعات»** (مع BOM)، وورقة الجودة/الإنتاج والفاقد باقيان في قسم «التصنيع».
- [ ] لا يبقى أى نصّ معروض «أوامر التشغيل/Work Orders» أو «المكتب الفني/Technical Office» للمستخدم.
- [ ] مفاتيح الجدول/الكلاس/الصلاحيات/الدور/المجموعة **لم تتغيّر**؛ الاختبارات القائمة كلها خضراء.
- [ ] i18n (EN==AR) + light/dark/RTL + وصفة الكاش اتنفّذت.

---

## ملحق — حالة باقي أجزاء الجولة

| الجزء | السلايد | الحالة |
|---|---|---|
| **إعادة التسمية + نقل القسم** | **1** | **هذا الملف — الجزء الوحيد المتبقّى.** |
| ورقة الجودة (مواصفات/checkboxes/قراءتان) | 2–4 | ✅ مُنفَّذ — `QualitySheet_Modifications_Plan.md`. |
| دورة اعتماد الأمر (Draft + بوابة مكتب المشروعات) | 5، 10 | ✅ مُنفَّذ — `PMO_ApprovalWorkflow_Plan.md`. |
| Standard BOM + المرونة اليدوية | 6 | ✅ مُنفَّذ — `PMO_StandardBOM_Plan.md`. |
| صرف من أمر التصنيع + تكلفة تلقائية | 7–8 | ✅ مُنفَّذ ضمن تغيير BOM. |
| تقرير الإنتاج والفاقد | 9 | ✅ مُنفَّذ. |
| إضافة التام للمخزون عند الانتهاء | 11 | ✅ مُنفَّذ. |

> **ملاحظة:** هذا الجزء عرضى بحت ومستقلّ — لا يعتمد على أى جزء ولا يكسر شيئاً؛ المفاتيح الداخلية كلها ثابتة. هو المكمّل الطبيعى لدورة الاعتماد لأنه يسمّى «مكتب ادارة المشروعات» صاحب بوابة الاعتماد الجديدة.
